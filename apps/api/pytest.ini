[pytest]
asyncio_default_fixture_loop_scope = function
# Only collect the real suite. ``fixtures/repo_workspace`` holds sample repos
# (with intentionally red tests) used as inputs to the coding-agent tests, so
# they must not be collected as part of this suite.
testpaths = tests
norecursedirs = fixtures .venv migrations
