alembic==1.13.3
asyncpg==0.31.0
fastapi==0.138.0
starlette>=1.3.1
email-validator==2.2.0
greenlet==3.1.1
httpx>=0.28.0,<1.0
litellm>=1.84.0
# LiteLLM's OpenRouter transport imports orjson at request time. Keep it an
# explicit runtime dependency so model calls cannot fail only after startup.
orjson==3.11.9
langfuse==4.13.2
passlib[bcrypt]==1.7.4
pydantic>=2.10.0,<3.0
pydantic-settings==2.5.2
PyJWT>=2.13.0
python-dotenv>=1.2.2
APScheduler==3.10.4
pytest>=9.0.3
pytest-asyncio>=1.3.0
aiosqlite==0.20.0
redis==5.0.8
SQLAlchemy==2.0.51
uvicorn[standard]==0.30.6
# Sprint 3 — connectors
cryptography>=42.0.0
playwright>=1.44.0
composio==0.15.0
boto3>=1.34.0
# E2B 2.x adds the provider-supported pause/resume lifecycle used by durable
# cloud-computer sessions. Pin it so lifecycle or sandbox-security semantics
# cannot drift between releases.
e2b==2.31.0
python-multipart>=0.0.9
# Document parsing & OCR
pypdf>=4.2.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
pypdfium2>=4.30.0
reportlab==5.0.0
Pillow>=12.2.0
# Production error reporting. The FastAPI extra keeps the supported framework
# integration in the fully hashed runtime dependency set.
sentry-sdk[fastapi]==2.64.0
# Stripe owns payment details and subscription collection; pin the server SDK so
# Checkout, portal, and signed-webhook behavior change only through review.
stripe==14.4.0
# Data analysis workspace (Phase 7 Task 7)
pandas>=2.0
matplotlib>=3.7
numpy>=1.24
