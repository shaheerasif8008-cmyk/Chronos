# Chronos Total Parity Matrix

## Purpose

This matrix is the controlling implementation contract for the Chronos Total Parity Program. It translates the north-star goal in `CHRONOS_TOTAL_PARITY_GOAL.md` into concrete, testable capability rows.

No parity claim is valid unless the matching row is marked implemented with proof. A row is complete only when it works through the real UI/API, persists durable state where applicable, respects tenant boundaries, emits audit evidence where applicable, survives refresh/restart where applicable, and has automated proof.

## Status Legend

- **Foundation present**: meaningful repo substrate exists, but the full product capability is not complete.
- **Partial**: user-facing or backend behavior exists for a subset of the target.
- **Missing**: no complete implementation found in the current checkout.
- **Planned**: deliberately listed as required parity work; no completion claim.

## Current Foundation Snapshot

Chronos already has foundations for chat, tool use, governed execution, scoped memory, browser search/fetch, Gmail drafting/search, filesystem/code tools, artifacts, approvals, connector framework, activity streams, settings, and task orchestration. These foundations are not enough for total parity. The rows below define the remaining product contract.

## Matrix Columns

- **Target parity**: what Chronos must provide.
- **Current state**: current repo/product state, without overstating readiness.
- **Implementation area**: subsystem that owns the work.
- **Interface and persistence**: public UX/API/state required.
- **Acceptance proof**: minimum proof before marking complete.

## Core Assistant and Chat

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| General chat assistant | ChatGPT/Claude-grade conversational assistant with streaming, history, model selection, citations, artifacts, memory refs, and tool traces. | E2E-proven for the core loop: send → live model stream → durable persistence (read back via API) → reply visible in UI. Proof: apps/web/e2e/chat.spec.ts (passes against real DeepSeek). Richer metadata persistence (citations, tool traces, memory refs) remains partial. | Chat UI, chat router, LLM layer, message model. | Conversation/message records include model, mode, citations, tool traces, memory refs, artifacts, status. | API and Playwright test: send message, stream answer, refresh, history and metadata persist. **Core loop done** via apps/web/e2e/chat.spec.ts. |
| Model selection | User can choose models/modes by task with safe fallback and clear provider status. | Implemented for current model surfaces: `/chat/models` returns labels, concrete model ids, capabilities, availability status, tool-use flag, fallback role, and policy text; composer persists selected model and exposes status metadata. Proof: tests/test_llm_and_memory.py::test_available_chat_models_expose_capabilities_status_and_policy, apps/web/e2e/model-selection.spec.ts, `npx next build --webpack`. | LLM config, chat composer, settings. | Model registry with label, provider, capabilities, status, policy constraints. | Model metadata test + model selection persists across reload. |
| Reasoning and tool mode control | User can select default, research, agent, browser, computer, data, image, voice, or coding modes. | Implemented for current mode surfaces: centralized mode registry, `/chat/modes`, persisted composer selection, status/capability metadata, and normalized mode persistence on chat/task records. Unavailable modes are explicitly labeled unavailable instead of silently pretending capability exists. Proof: tests/test_chat_modes.py, `npx next build --webpack`. | Chat composer, router, runtime planner. | Message/task mode persisted and shown in UI. | Backend mode registry/persistence tests + web build. |
| Chat controls | Edit prompt, regenerate, branch, retry from here, pin, copy, export, save to memory, convert to task/workflow. | Implemented for current message controls: pin/unpin, copy, edit user message, branch, save-to-memory, convert-to-task, convert-to-workflow, export, regenerate assistant response, and retry-from-here now have UI actions and backend/API support where persistence is required. Proof: tests/test_chat_controls.py, apps/web/e2e/chat-controls-static.spec.ts, `npx next build --webpack`. | Chat UI, message actions, task/workflow APIs. | Message action endpoints and branch/fork lineage. | Backend chat-control tests + static UI route/action guard. |
| Conversation search | Search across conversations, tasks, artifacts, memory, and sources. | Missing as unified product feature. | Search service, UI global search. | Search index or queries over persisted entities with permission filters. | Search returns only authorized results with type filters. |

## Runtime, Agent, and Reliability

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Native tool loop | Model-native decide-act-observe loop with parallel tool calls and honest failure handling. | Implemented (Phase 1): runtime agent loop with persisted iteration history, tool calls/results/errors, injection-gated writes, and cancellation checks. Proof: tests/test_inline_tools.py and tests/test_runtime_reliability_phase1.py::test_run_loop_gates_write_after_untrusted_prompt_injection, ::test_run_loop_stops_before_model_step_when_task_is_cancelled. | Runtime agent loop, tool registry, broker. | Persisted agent history, iteration count, tool calls/results/errors. | Multi-step loop, tool error handling, injection gate (tests above). |
| DAG execution | Complex task DAG with dependencies, conditions, parallel execution, checkpoints, and replanning. | Implemented (Phase 1): executor/planner with step state, conditional skip, checkpoints, and resume. Proof: tests/test_orchestration_category1.py, tests/test_state_management_category5.py. | Runtime executor, planner, task state. | Task plan, completed/skipped steps, result context, checkpoint records. | Conditional skip, step state, resume (tests above). |
| Task queue | Priority queue, per-org/user/tool concurrency, worker limits, dead-letter state. | Implemented (Phase 1, single-instance architecture): priority runner, concurrency cap, queued status, startup re-enqueue, and now **dead-letter state** — exhausted-retry/timeout tasks set `dead_letter=true` with a `failure_reason` taxonomy and `attempts` count (migration 0025), listable via `GET /tasks?dead_letter=true` and revivable via `POST /tasks/{id}/retry`. Distributed worker leases are a deliberate **non-goal** per CLAUDE.md ("APScheduler in-process, not Celery"; "one Chronos instance per organization"), not a deferral. Proof: tests/test_runtime_reliability_phase1.py::test_exhausted_retries_dead_letters_with_error_taxonomy, ::test_requeue_clears_dead_letter_and_reruns, ::test_task_runner_executes_higher_priority_before_normal_priority. | Runtime task runner, Postgres queue state. | Queue records, priority, attempts, dead-letter flag. | Priority ordering, concurrency cap, dead-letter + requeue (tests above). |
| Cancellation | User can cancel running tasks and tool work stops promptly. | Implemented (Phase 1): task cancel API, queued-task cancellation, native-loop/DAG cooperative cancellation checks, durable cancellation activity + `failure_reason="cancelled"`. Cancellation propagates via asyncio task cancellation (raises at the next await in the loop/tool); browser/computer adapter interruption lands with those subsystems (Phase 9/10). Proof: tests/test_runtime_reliability_phase1.py::test_task_runner_cancel_queued_task_marks_cancelled_without_execution, ::test_run_loop_stops_before_model_step_when_task_is_cancelled. | Runtime runner, native loop, UI controls. | Cancellation status persisted; trace records cancellation. | Cancel before model step verified; no further steps execute (tests above). |
| Timeouts and retries | Step-level and task-level timeouts, retry policy, final failure taxonomy. | Implemented (Phase 1): settings-backed max attempts + task timeout in the runner; **final failure taxonomy** (`timeout`/`cancelled`/`error`) persisted on the task with attempt count. Connector-specific retry policies continue under the connector framework. Proof: tests/test_runtime_reliability_phase1.py::test_task_runner_retries_failed_task_before_marking_failed, ::test_timeout_dead_letters_with_timeout_taxonomy, ::test_classify_failure_taxonomy. | Runtime runner, settings. | Per-task policy, retry count, failure_reason. | Timeout, retry-then-fail, taxonomy classification (tests above). |
| Crash recovery | Recover after restart for every task/session type that exists. | Implemented (Phase 1) for all existing surfaces: startup re-enqueues queued/pending/planning/running tasks, approval decisions enqueue resume exactly once, and connector workflow recovery runs at startup. **Scope note:** the original target named "browser/computer sessions"; those session managers do not exist yet (Phase 9/10, Missing), so their recovery is N/A until they ship — this row covers native loop, DAG, approvals, connector jobs, and scheduled tasks. Proof: tests/test_runtime_reliability_phase1.py::test_create_task_queues_task_instead_of_fire_and_forget plus startup recovery in main.py (`recover_incomplete_tasks`/`recover_incomplete_workflows`). | Startup recovery, task runner. | Durable task/session state sufficient for resume or safe fail. | Restart re-enqueues incomplete tasks; approval resumes exactly once. |
| Durable trace replay | Full task timeline replayable after refresh/restart. | Implemented (Phase 1): replay-significant activity events (tool calls/results, approvals, artifacts, sub-agents, task completion/failure/cancel incl. dead-letter, route/model steps) persist to append-only audit and replay after restart. High-frequency heartbeat stays Redis-only **by design** (durable trace carries replay-significant events, not per-tick liveness). Proof: tests/test_runtime_reliability_phase1.py::test_publish_activity_persists_replayable_model_trace **and E2E** apps/web/e2e/activity.spec.ts (create task → complete → open Activity timeline → refresh twice → persisted event count matches). | Trace persistence, activity API, UI timeline. | Durable events for model, tools, artifacts, approvals, completion. | Replayable trace persisted (backend test) + refresh-stable timeline (E2E activity.spec.ts). |
| Idempotent writes | External write actions execute at most once across retries/recovery. | Implemented (Phase 1) at the broker seam: write tools accept idempotency keys, replay cached provider responses across retry/recovery, and audit redacted idempotency evidence. Per-connector provider-native idempotency is additive as each connector lands (the seam, not each adapter, is the Phase 1 deliverable). Proof: tests/test_runtime_reliability_phase1.py::test_tool_broker_reuses_idempotent_external_write_result. | Tool broker, connector adapters. | Idempotency key per write action and provider response reference. | Retry around approved write does not duplicate (test above). |

## Governance, Safety, and Audit

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Permission seam | Every action uses `permission.check`; future policy engine can replace implementation without call-site changes. | Foundation present. | Permissions core, routers, runtime. | Audit of permission decisions and tenant/resource context. | Static grep plus tests for protected routes/tools. |
| Tool broker seam | Every external action routes through `tool_broker.execute`. | Foundation present; W3 added centralized connector-rate governance and loop-triggered org suspension at the broker boundary. | Broker, tool registry, connectors. | Tool call/result audit with redacted args. | Static grep plus tool execution tests; W3 governance tests for rate/cost admission. |
| Approval system | Risky writes pause, route to authorized approver, resume exactly once, reject cleanly. | Proven deterministically: real broker gate (gmail.draft → ApprovalRequired via gmail provider policy) + HTTP decide→approved→resume in apps/api/tests/test_approval_flow_http.py; real inbox→Approve UI flow in apps/web/e2e/approvals.spec.ts (seeded). NOTE: "unauthorized cannot approve" is NOT yet proven — permission.check only enforces with OpenFGA, which is off in this env. The live agent-mode trigger is non-deterministic (model-dependent) so it is not used in CI. | Approvals router/UI, broker policy. | Approval records, payload, status, actor, resume result. | E2E: unauthorized cannot approve; authorized approval resumes task once. **Authorized approve→resume done; unauthorized-block pending OpenFGA.** |
| Audit log | Searchable/exportable audit for tool, memory, approval, policy, connector, task, and admin events. | Foundation present; searchable/exportable audit product incomplete. | Audit service, admin UI. | Append-only audit with redaction and export endpoints. | Test export includes expected chain and no secrets. |
| Prompt injection defense | Untrusted browser/file/email/connector content cannot issue instructions or trigger actions. | Phase 1 foundation implemented: browser and read-like connector outputs are marked untrusted, prompt-injection markers are surfaced, native loop gates writes after injected content, and broker rejects runtime-flagged untrusted-triggered writes without approval. Broader file/email/OCR ingestion wrapping expands as those ingestion surfaces ship. | Content ingestion, LLM context assembler, broker policy. | Untrusted content wrappers, scan result, action grounding record. | Injection fixture cannot cause email/send/post/delete or policy bypass. |
| Tenant isolation | Every API/query/tool/artifact/memory path enforces organization and permission scope. | Proven at multiple layers: search org-isolation (test_search.py), in-process artifact cross-org (test_artifact_workspace.py), HTTP-boundary cross-org 404 over the real ASGI app with real auth/tokens (test_tenant_isolation_http.py), and W3 tenant-bound vault reads by `(organization_id, vault_ref)`. Broader per-resource HTTP coverage (tasks/connectors/projects) still expanding. | DB queries, routers, broker, storage, vault. | Org/region on every persisted row and storage/cache path. | Cross-tenant tests for memory, artifacts, tasks, connectors, projects. **Artifacts proven over HTTP** via test_tenant_isolation_http.py; vault boundary covered by test_w3_governance.py. |
| Secret handling | Credentials encrypted, never logged, revocable, and redacted in traces. | W3 hardened the vault read seam: `vault.get` requires `org_id`, cache keys are tenant-qualified, durable lookup filters by organization before decrypting, and Gmail/generic OAuth/framework credential loaders pass tenant context. Raw connector args/results remain redacted in broker audit. | Vault, connectors, audit redaction. | Vault refs only in logs; rotation/revoke state; tenant-bound credential cache. | Redaction test suite and connector revoke proof; W3 vault tenant-bound proof in test_w3_governance.py. |
| Degraded-mode honesty | Fixture/demo/fallback data is never presented as live. | Foundation present in browser warning behavior; must cover all connectors. | Connector framework, UI status. | Result metadata includes tier/provider/fallback reason. | Tests assert fallback warning shown and answer reports limitation. |

## Projects and Knowledge Sources

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Projects | Claude/ChatGPT-style projects with instructions, members, conversations, sources, memory, tasks, artifacts, and tools. | Missing as first-class product surface. | Projects API/UI, DB schema, context assembly. | `projects`, memberships, instructions, settings, linked entities. | E2E create project, add member/source/chat/artifact, enforce access. |
| Project instructions | Project-level instruction layer merged safely with system/user context. | Partial via context folder and persona/workspace fields; no project product. | Context assembler, projects. | Instruction version and audit of edits. | Test project instruction affects answers only inside project. |
| Source upload | Upload PDFs, docs, slides, sheets, text, code, images, URLs. | Missing as general source upload workflow. | Upload service, storage, parser pipeline. | Source record, object path, parse status, extracted text, chunks. | Upload PDF/CSV/code folder and see indexed status. |
| Source indexing | Chunk, embed, refresh, remove, reindex, cite, permission-resync sources. | Partial memory embeddings exist; project source indexing missing. | Ingestion pipeline, embeddings, retrieval. | Source chunks, embeddings, permissions, freshness. | Ask source-grounded question with cited chunk; remove source removes retrieval. |
| Connector-synced knowledge | Drive/SharePoint/Notion/GitHub/Slack/Gmail/Teams indexed as project/org sources. | Implemented (Phase 8): connector feed sources fetch through `tool_broker.execute`, normalize provider payload shapes (`documents`/`threads`/`messages`/`items`/`value`/etc.), save durable connector document artifacts, index chunks through the project source pipeline, mark failed/revoked states honestly, delete chunks on revocation, and are now usable by research as `connector` citations when indexed. Proof: tests/test_source_sync.py and tests/test_deep_research.py::test_connector_indexed_sources_are_used_as_research_citations, ::test_no_fabricated_connector_citation. | Connector sync jobs, retrieval, projects, research. | Sync state, source permissions, chunk index, failure/revoked status, citation source_type. | Sync fixture connector, cite connector result in research, revoke permission removes chunks/access. |
| Source viewer | Inspect original, extracted text, chunks, warnings, index status. | Missing. | Project source UI, artifacts/storage. | Source metadata and preview endpoint. | Playwright source viewer and reindex/delete controls. |

## Memory

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Scoped retrieval | Authorized multi-signal memory retrieval with query expansion, ranking, dedupe. | Foundation present. | Memory core, embeddings, requester context. | Memory rows with scope/source/importance/provenance. | Tests for personal/workspace/org/restricted retrieval. |
| Explicit memory | User says remember/forget/edit and memory updates immediately. | E2E-proven: add (remember) → retrieve (list) → edit → delete through the real memory UI, stable across reruns. Proof: apps/web/e2e/memory.spec.ts. | Chat route, memory router/UI. | Memory entry and audit event. | E2E remember, retrieve, edit, delete. **Done** via apps/web/e2e/memory.spec.ts. |
| Autonomous memory | Assistant extracts useful durable facts visibly with undo. | Partial: extraction exists; inline visibility/undo completeness uncertain. | Extraction job, chat UI. | Candidate memory, source conversation, undo event. | Test autonomous memory appears, undo removes retrieval. |
| Memory control center | Search/edit/delete/archive/merge/pin/scope/import/export, usage logs. | Implemented (Phase 4): `core/memory_control.py` + `/memory` endpoints for list/search, archive, pin, classify-sensitive, change-scope, merge, export/import, and usage logs; memory_usage_log table records every retrieval; control-center UI (search, pin/sensitive/archive actions, export/import) in `MemoryScreen`. Pinned float in ranking; archived excluded from retrieval. Proof: tests/test_memory_parity.py::test_export_import_round_trip, ::test_control_flags_are_tenant_scoped, ::test_search_filter_in_list, ::test_usage_log_records_retrieved_memories. | Memory UI/API, import/export. | Memory status, provenance, usage records. | DB-backed pytest above; UI typecheck-verified via `next build`. |
| Memory conflict/staleness | Detect stale/conflicting memories and propose merge/replacement. | Implemented (Phase 4): deterministic near-duplicate detection (token Jaccard within scope) via `detect_conflicts`; `superseded_by` column marks stale entries, which are excluded from retrieval; `merge`/`resolve_conflict` supersede losers; conflicts review panel in UI. Proof: tests/test_memory_parity.py::test_conflict_detection_and_resolution, ::test_archived_and_superseded_excluded_from_retrieval. | Memory ranking, supersession. | Conflict links, stale flag, resolution history. | Newer preference supersedes and suppresses old conflicting one (test above). |
| Memory privacy controls | Disable memory per project/chat/user, classify sensitive memories. | Implemented (Phase 4): per-org/project/member/conversation disable via `memory_policy` in `settings_documents`; `is_memory_enabled` gates **both write and retrieval** — explicit `POST /memory/` (403) and autonomous `extraction.extract_and_save` (returns before any model call/write, enforcing conversation-level too) are blocked when disabled, and `memory.retrieve` (frozen seam, additive) enforces org/project/member. `is_sensitive` provides manual sensitive classification. Note: retrieval cannot enforce *conversation*-level disable today (the frozen `retrieve` signature carries no conversation_id) — conversation-level is enforced on the write/extraction path. Proof: tests/test_memory_parity.py::test_privacy_disable_blocks_retrieval_and_is_scoped, ::test_disabled_memory_blocks_explicit_write, ::test_disabled_memory_blocks_autonomous_extraction_write. | Settings, memory policy, retrieval + write seams. | Policy settings and classification. | Disabled memory prevents write (explicit + autonomous) and retrieval, scoped per project (tests above). |

## Artifacts

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Artifact creation | Assistant creates durable reusable outputs from chat/tasks. | Implemented (Phase 5): create via POST /artifacts and runtime save_artifact; version-addressed storage; list/download/duplicate; route boundary enforces tenant isolation (404 cross-org). Proof: tests/test_artifact_workspace.py::test_create_read_download_roundtrip_survives_refetch, ::test_duplicate_creates_independent_copy, ::test_router_blocks_cross_org_access. | Artifact core/router, runtime. | Artifact row, object storage/local fallback, message/task linkage. | E2E create artifact, refresh, download. |
| Artifact workspace | Side-by-side preview, full artifact browser, project/task grouping. | Implemented (Phase 5): full artifact browser (search + kind filter + conversation/task grouping), two-pane workspace, Preview/Edit/Versions tabs, version timeline, diff viewer, plus an in-chat side panel (open/edit artifacts without leaving chat). Typecheck-verified via npm run build; route /artifacts (behavioral E2E deferred to harness). | Artifacts UI/API. | Artifact collections and filters. | Playwright artifact browser, permissions, refresh. |
| Artifact editing | User and AI can edit artifacts without destructive overwrite. | Implemented (Phase 5): non-destructive versioning (artifact_versions), manual edit, AI edit, restore, unified diff — all via permission seam + audit. Proof: tests/test_artifact_workspace.py::test_edit_creates_new_version_without_clobbering **plus UI E2E** apps/web/e2e/artifacts.spec.ts (create → preview → edit → new version → diff → restore through the real workspace; stable across repeated runs). | Artifact editor, version API. | `artifact_versions`, diff metadata. | Edit creates new version; restore works. **Done** (backend unit + UI E2E). |
| Artifact renderers | Markdown, HTML, React, SVG, diagrams, CSV/table, JSON, code, image, docs/sheets/slides. | Implemented (Phase 5): markdown/code/json/csv-table/image renderers + sandboxed iframe (no allow-same-origin) for HTML/SVG with CSP; download fallback for binary/office types. Proof: components/artifacts/ArtifactRenderer.tsx; typecheck-verified via npm run build (behavioral render E2E deferred to harness). | Renderer UI, sandbox. | Renderer selection and safe preview policy. | Renderer tests per supported type. |
| Artifact publish/share | Controlled share/publish/unpublish with permissions. | Implemented (Phase 5): governed publish/unpublish with signed-token public /shared/{token} link and revocation; seam-gated + audited. Proof: tests/test_artifact_workspace.py::test_public_share_boundary_serves_then_revokes. | Sharing API/UI, permissions. | Share links, ACL, publish status. | Unauthorized cannot access; unpublish revokes. |

> Phase 5 proof strategy: backend behavior is proven by DB-backed pytest against an isolated database. A Playwright behavioral E2E harness now exists in the repo (`apps/web/playwright.config.ts` + `apps/web/e2e/`, isolated dual-server with real dev-OTP auth); the artifact edit/version/diff/restore lifecycle is covered by `apps/web/e2e/artifacts.spec.ts`. Rows not yet covered by an E2E spec remain typecheck-verified via `npm run build`.
>
> Phase 5 lifecycle coverage: create, preview, edit, AI edit, version, diff, restore, rename, **duplicate**, **move**, delete (soft), publish/unpublish (revocable share link). **`move` is now implemented** (Projects landed in Phase 3): artifacts carry a direct `project_id` (migration 0023), the `POST /artifacts/{id}/move` endpoint re-homes them (tenant-scoped, audited), and the project artifacts query unions directly-moved artifacts. Proof: tests/test_artifact_workspace.py::test_move_artifact_into_project_and_unlink, ::test_move_rejects_cross_org_project_and_artifact.

## Deep Research

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Research mode | Dedicated quick/standard/exhaustive/trusted research runs. | Implemented (Phase 6): durable `research_runs` (migration 0026) with question, depth (quick/standard/exhaustive/trusted), source scopes, citation policy, time budget, plan, status, findings, limitations; standalone executor (`runtime/research_executor.py`) plans→gathers→synthesizes→reports with cooperative cancellation and startup recovery (`main.recover_incomplete_research`); `/research` router (create/list/get/citations/events/stream/cancel) and a `ResearchScreen` UI with composer, status, polled timeline, citations, cancel, and report viewer. Proof: backend `tests/test_deep_research.py::test_run_lifecycle_completes_with_report_artifact` (+ HTTP create/cancel/cross-org/recovery tests) **and a live full-stack run** (real OpenRouter model + live web search: quick run → complete → 6 web citations → cited report artifact, durable event timeline replays). | Research API/UI, planner, runtime. | Research run, plan, source scope, depth, status. | E2E web research with cited report. **Done** — DB-backed lifecycle test + live full-stack run + behavioral `apps/web/e2e/research.spec.ts` **passing** under the dual-server harness (real model + live web search: composer → run reaches `complete` → report opens → survives reload). |
| Citation collector | Store source snippets, metadata, confidence, limitations. | Implemented (Phase 6): `research_citations` rows carry marker, source_type, title, url, snippet, confidence, distance, and metadata (incl. untrusted-content scan). `core.research.add_citation` enforces the **no-citation-without-snippet invariant** (raises on empty/whitespace snippet before any write); run-level `limitations` capture degraded/unavailable sources. Proof: `tests/test_deep_research.py::test_add_citation_requires_snippet`, `::test_citations_and_runs_are_tenant_scoped`; live run produced 6 snippet-backed citations that the report cites via [S#]. | Retrieval/browser/research. | Citation records linked to report/message/artifact. | **Done** — no citation without source snippet (test); report cites stored sources (live run). |
| Internal + external research | Merge project files, web, connector sources, uploads, MCP. | Implemented (Phase 6/8) **source-type-agnostic**: `web` via `tool_broker.execute(browser.search/fetch)`, `project` via `retrieve_source_chunks`, and indexed `connector` sources are wired and proven. `upload`/`mcp` scopes are accepted but emit an honest `research_source_skipped` event + limitation until those indexes exist (matrix rows *General file upload* line 113 and *Custom MCP* line 132) — **citations are never fabricated for an unavailable source type**. Proof: `tests/test_deep_research.py::test_internal_external_merge` (project + web citations in one run), `::test_connector_indexed_sources_are_used_as_research_citations`, `::test_no_fabricated_connector_citation`, `::test_degraded_search_records_limitation`. | Research planner, retrieval, connectors. | Source scope and retrieval provenance. | **Done for project + web + indexed connector sources**; MCP/upload remain truthful degraded modes until those indexes ship. |
| Research report artifact | Export report to artifact/document/PDF with source table. | Implemented (Phase 6): the report is saved as a durable markdown artifact via `save_artifact` (linked to the run's project when set) with a `## Limitations` section and a `## Sources` appendix table built from stored citations; `report_artifact_id` persists on the run. PDF export is **optional** (not required by the row proof); markdown artifact + source table satisfies it. Proof: `tests/test_deep_research.py::test_run_lifecycle_completes_with_report_artifact` (artifact contains `## Sources`, survives engine dispose / re-read); live run produced a 4.6 KB report reopened via `/artifacts/{id}`. | Research UI, artifacts, export. | Report artifact and source appendix. | **Done** — download/open report after refresh (re-read test + live run + UI report viewer). |

## Multimodal, Files, and Data

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| General file upload | Attach files to chat/project/task/research/artifact. | Implemented (Phase 7): `/attachments` uploads → `save_artifact(kind="attachment")` with object path, links to conversation/project/**task**/**research run** (tenant-scoped, cross-org 404, audited); uploaded `attachment_ids` persist onto the user message (`messages.artifact_refs`) and round-trip through history so attachment chips reappear after refresh; composer surfaces upload failures. Proof: tests/test_file_upload.py (persist-after-refresh, task link+audit, cross-org 404 for conversation/task/research), `npm run build`. | Upload service, storage, UI composer. | Uploaded file record, object path, linked entity. | Upload file, refresh, file remains attached. **Done** via tests/test_file_upload.py + web build. |
| Document intelligence | Parse/summarize/compare/extract PDFs, DOCX, PPTX, XLSX, CSV, code, text. | Implemented (Phase 7): on top of the existing `parsing/engine.py` + `doc.parse`/`doc.read`, added broker-routed `doc.summarize` and `doc.compare` (provider `doc`, run through `tool_broker.execute` with permission + audit). Each produces an LLM summary/comparison **with citations validated against the parsed source** — every claim's verbatim quote is located in `full_text` and any claim whose quote is not found is dropped from BOTH the citation list AND the returned summary/comparison output (no fabricated spans). Unparseable docs and LLM-infra failures return an honest warning with no fabricated content; parser warnings (`note`/`parser_used`/`truncated`) surfaced in tool results. Proof: tests/test_document_intelligence.py (PDF/DOCX/XLSX/CSV summarize+compare with `full_text[start:end]==quote` span checks, discriminating fabricated-quote drop tests, unparseable + wrong-shape-JSON + LLM-failure honest-degrade), no regression in tests/test_doc_parsing.py. | Parsers, ingestion, LLM, artifacts. | Extracted text/tables/slides and parser warnings. | Tests per file class with cited extraction. **Done** via tests/test_document_intelligence.py. |
| Image input | Vision for screenshots, diagrams, charts, UI inspection, OCR. | Implemented (Phase 7): true multimodal chat — `core/llm.model_supports_vision` marks vision-capable chat models (gpt-5.4-mini/nano), and `_build_vision_blocks` (chat router) resolves org-owned image attachments into OpenAI-style `image_url` base64 content blocks assembled into the user turn via `core/context.build_user_turn_content`, so the model reasons over the actual image. Tenant-scoped (cross-org images excluded), per-image size cap (5MB) to stay within provider body limits. Honest degraded path when the model lacks vision: OCR-extracted text is embedded in the turn and a truthful "vision unavailable — used OCR" note is attached **only when extracted text exists** (no overclaim). Persisted image attachments render as thumbnails in the chat message. Proof: tests/test_image_input.py (vision path builds image_url block decoding to stored bytes; non-vision → OCR text + honest note, no fabricated block; cross-org excluded via the real `_build_vision_blocks`; oversized skipped; `model_supports_vision` booleans), `npm run build`; no regression in test_chat_turn/test_file_upload/test_llm_and_memory. | LLM provider abstraction, upload, message renderer. | Image attachment and extracted metadata. | Screenshot question returns grounded answer. **Done** via tests/test_image_input.py. |
| Image generation | Generate images with style/size/count controls. | Implemented (Phase 7): broker-routed `image.generate` (provider `image` in `tool_broker._route`) → `connectors/image_gen.py` provider abstraction behind a single stubbable `_call_provider` (litellm `aimage_generation`) → generated images saved as `kind="image"` artifacts with generation metadata (prompt/size/count/style/model/provider). Broker enforces a count safety cap (max 4, int+range validated); generative action (not approval-gated). Honest degraded leaf: empty `settings.image_model`, provider error, or empty provider result all return a truthful unavailable/error status with NO fabricated image (no artifact). Chat **Image mode** invokes the broker and renders the resulting image artifact inline (degraded text shown when unavailable). Proof: tests/test_image_generation.py (artifact created+reopened after refetch with metadata; count cap raises; honest no-provider/provider-error/empty-result paths; cross-org isolation via the **real** `routers.artifacts._require` guard; registry presence), no regression in test_inline_tools/test_runtime_reliability_phase1/test_chat_modes. Live provider call is honest-degraded (no image key in env). | Image provider tool, artifacts. | Image artifact and generation metadata. | Generate image artifact and reopen. **Done (honest degraded)** via tests/test_image_generation.py. |
| Image editing | Edit/variation/masking/background operations where provider supports. | Implemented (Phase 7): broker-routed `image.edit` in `connectors/image_gen.py` (reuses Task 4's `image` provider) → resolves the source image artifact (org-checked, cross-org rejected), optional mask (artifact id with org check, or inline base64), edit prompt + operation → single stubbable `_call_edit_provider` → output written as a **new NON-DESTRUCTIVE version** of the source artifact via `core/artifact_versions.create_version` (prior version bytes preserved). Edit appears in the existing Phase 5 version timeline/diff/restore. Honest degraded leaf: empty `settings.image_model`, provider error, empty result, and version-creation failure all return a truthful error/unavailable status with NO new version and no exception propagation. Proof: tests/test_image_editing.py (new version created + **original bytes survive at prior version**; honest no-provider/provider-error/version-failure/cross-org paths; `_resolve_mask` all four branches incl. cross-org mask ignored), no regression in test_image_generation/test_artifact_workspace/test_inline_tools. Live provider call honest-degraded (no image key in env). | Image provider tool, artifact versions. | Source image, edit params, output artifact/version. | Edit creates new image version. **Done (honest degraded)** via tests/test_image_editing.py. |
| Voice input/output | Speech-to-text, TTS, hands-free mode, audio artifacts/transcripts. | Implemented (Phase 7): broker-routed `voice.transcribe` (STT) and `voice.speak` (TTS) in `connectors/voice.py` (provider `voice` in `tool_broker._route` + vault-bypass), each behind a single stubbable seam (`_call_stt`/`_call_tts`). STT takes an uploaded audio artifact (org-checked; cross-org rejected) or inline `audio_b64`, and persists the transcript as a `kind="transcript"` artifact; TTS saves a `kind="audio"` artifact with TTS metadata (model/voice/text preview). Tenant-scoped `/chat/voice/transcribe` + `/chat/voice/speak` endpoints (member-authed, broker-routed, audited, conversation-membership-checked); chat UI adds an audio-upload→transcribe control (transcript fills composer) and a per-message TTS play button (blob-URL lifecycle managed). Honest degraded leaves: empty `stt_model`/`tts_model`, provider error, empty result → truthful unavailable/error with NO artifact and no propagation. (Audio-file upload, not live mic capture.) Proof: tests/test_voice.py (transcript+audio artifacts persisted+refetched; honest no-provider/provider-error paths; cross-org audio rejected; inline audio_b64 + malformed-b64 honest error; endpoint cross-org conversation 404), no regression in test_image_generation/test_inline_tools/test_runtime_reliability_phase1; `npm run build`. Live STT/TTS honest-degraded (no voice keys in env). | Voice provider, chat UI, storage. | Audio attachment, transcript message, TTS metadata. | Record/upload audio, transcript persists, TTS metadata present. **Done (honest degraded)** via tests/test_voice.py. |
| Data analysis workspace | Python-backed analysis of CSV/XLSX/JSON with tables, charts, reports. | Implemented (Phase 7): pandas/matplotlib/numpy added; broker-routed `data.run` tool (`connectors/data_analysis.py`, provider `data` + vault-bypass) runs analysis code in a subprocess sandbox (Agg backend; allows pandas/matplotlib/numpy; blocks network/subprocess/ctypes/dynamic-import incl. importlib·builtins/eval·exec·compile/os.fork/abs-path open; RLIMIT CPU 15s/AS 2GB/FSIZE 25MB/NPROC 64; 20s wall-clock with process-group kill; per-run workspace always cleaned up). `datasets` table + migration 0027 (org+region NOT NULL); `routers/data.py` — `POST /datasets` (pandas schema+row_count inference, source-size capped), tenant-scoped `GET /datasets[/{id}]` (cross-org 404), broker-routed audited `POST /datasets/{id}/analyze`. Only the dataset's own org-checked source file is materialized into the sandbox. Charts→image artifacts, stdout→report artifact (org-scoped, persisted). `/data` UI screen: upload→dataset→inferred schema→run→view generated chart/report artifacts. Proof: tests/test_data_analysis.py (real-subprocess chart+report artifacts persisted+refetched; schema inference; cross-org tool+HTTP rejection; 13-vector sandbox-escape validator incl. importlib/builtins/eval/exec; imports available), no regression in test_inline_tools/test_runtime_reliability_phase1/test_doc_parsing; `npm run build` with `/data`. Note: regex sandbox is defense-in-depth atop subprocess+RLIMIT+stripped-env+`-I`; deeper OS isolation (seccomp/uid namespaces) is follow-up. | Code sandbox, data UI, artifacts. | Dataset record, generated charts/reports. | Upload CSV, generate chart/report artifacts. **Done** via tests/test_data_analysis.py. |

## Connectors and Apps

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Connector directory | App catalog with setup, scopes, actions, risk, health, policy, audit. | Implemented (Phase 8): `/connectors/catalog` exposes setup env vars, auth type, scopes, actions, risk levels, sync support, policy text, connected account, health status, and last-used state; the Connectors UI renders this metadata and supports OAuth connect/reconnect/disconnect while being truthful for non-OAuth setup. Framework connector install→actions→policy→queued execution→health→audit is proven deterministically. Proof: apps/web/e2e/connectors.spec.ts (directory + connected app), tests/test_connector_phase8.py, tests/test_connector_framework.py, tests/test_connector_operations.py. | Connectors UI/API, repository, health, execution logs. | Connector definitions, installations, policies, health, last-used audit logs. | Connect app fixture, see health/actions/risk/sync/policy, install and execute through framework. |
| Gmail | Search/read and draft/send-with-approval. | Implemented at governed connector level: Gmail remains the dedicated broker adapter for search/draft/send policy, and Phase 8 now also exposes catalog setup/scopes/actions/risk/policy metadata plus framework install/action/policy proof. | Gmail connector, broker, approvals, connector directory. | Connector credential, drafts, approval records, health/audit metadata. | Search/draft/send path according to policy plus catalog/framework tests. |
| Google Drive | Search/read/create files; synced knowledge. | Implemented at generic Phase 8 connector level: catalog + OAuth setup, framework read/search/write action schemas, approval-required write action, sync-supported metadata, and connector-source indexing path for cited knowledge. Provider-specific live Drive endpoint smoke remains credential-dependent. | OAuth connector, framework adapters, sync/index. | Drive source records, action schemas, source chunks/citations. | Framework action schema/policy proof + connector indexed citation proof. |
| Google Calendar | Read/create/update events with approval policy. | Implemented at generic Phase 8 connector level: catalog + OAuth setup, framework read/search/write action schemas, and approval-required write action. Provider-specific live Calendar event smoke remains credential-dependent. | OAuth connector actions, framework policy. | Calendar action records, approvals, audit/health. | Framework action schema/policy proof. |
| Slack/Teams | Search/read/send/publish agents to channels. | Implemented at generic Phase 8 connector level for both Slack and Teams: catalog entries, OAuth setup, framework read/search/write actions, external-message risk where applicable, sync-supported metadata, and connector-source research citations. Agent publishing is covered by later Agent Publishing row. | Connectors, framework adapters, source sync. | Channel/message action schemas, message events, indexed source records. | Framework action schema/policy proof + connector indexed citation proof. |
| GitHub | Search/read repos/issues/PRs, code tasks, PR creation. | Implemented at generic Phase 8 connector level: catalog + OAuth setup, framework read/search/write actions, policy/audit/health, and sync-supported metadata. Full repo workspace/PR workflow remains owned by Coding Agent rows. | GitHub connector, framework adapters, coding agent later. | Repo/read/write action schemas, audit/health. | Framework action schema/policy proof. |
| Notion/Linear/HubSpot/Airtable/Jira/Salesforce/Stripe | Typed read/write actions with health and policies. | Implemented (Phase 8): all listed providers are present in the catalog and seeded as framework connectors with read/search/write action schemas, risk levels (including financial/external-message where needed), approval-required write paths, health updates, and audit logs. Proof: tests/test_connector_phase8.py, tests/test_connector_framework.py, tests/test_connector_operations.py. | Connector adapters. | Action schemas, approvals, audit, health. | Per-connector catalog/framework smoke for read/search and approval-gated write. |
| Custom MCP | Remote MCP setup, discovery, allow/deny, health, policy. | Implemented as setup/discovery/policy product surface: remote MCP appears in the catalog, `/connectors/mcp/register` stores remote/local server config, discovery calls the MCP transport and persists discovery logs, connector policies provide allow/deny/approval decisions, and execution still fails honestly until a discovered tool is explicitly wired through policy. | MCP connector framework. | Server config, discovered tools, policy, discovery logs. | Discover tool/log result and gate risky action via connector policy. |
| Custom HTTP/OpenAPI | User-defined API connector from OpenAPI/manual schema. | Implemented at framework level: custom HTTP appears in the catalog with API-key setup metadata and seeded `discover_schema` + approval-gated `request` actions; requests run through connector schema validation, permission/policy, redacted audit, queued execution, and health. Live OpenAPI import UI can build on this schema/action seam. Proof: tests/test_connector_phase8.py. | Connector builder/framework. | Auth config, action schemas, test calls, audit/health. | Import/inspect schema action and execute request through broker-governed connector framework. |

## Browser, Computer, and Autonomous Operation

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Browser search/fetch | Web search/fetch with screenshots and fallback honesty. | Foundation present. | Browser connector, broker, storage. | Tool result includes provider/tier/warnings; screenshots stored. | Live/fixture search tests and screenshot persistence. |
| Full browser operator | Navigate/click/type/select/scroll/wait/extract/download/upload/read DOM. | Implemented (Phase 9): broker-visible `browser.*` operator tools route to a persistent session manager with current URL/title, screenshot metadata, downloads, storage-state reference, consent scope, history, and replay events. If Playwright is not installed in the API runtime, sessions remain durable and truthfully report degraded metadata instead of pretending live control. Proof: `tests/test_browser_operator_phase9.py`, `tests/test_inline_tools.py`, `npx next build --webpack`. | Browser session manager, tool registry, UI. | `browser_sessions` and `browser_session_events`, `/browser-sessions` API, `/browser` UI. | Deterministic backend proof for tools/session/download-history/timeline replay plus web route/build proof; live form-fill E2E can run when Playwright/API harness is present. |
| Live browser view | User sees browser session and task actions. | Implemented (Phase 9): `/browser` product surface lists active/recent sessions, shows current URL/status/takeover state, renders screenshot data when present, and replays browser events from durable session history. Proof: `apps/web/e2e/browser-operator-static.spec.ts` using `e2e/static.config.ts`, `npx next build --webpack`. | Browser UI, streaming screenshot/WebRTC/VNC equivalent. | Session state and screenshots/video frames. | Static route/API guard and production build; browser event replay is backed by `browser_session_events` and task activity rows. |
| User takeover | Pause automation for user click/type/MFA/CAPTCHA, then hand back. | Implemented (Phase 9): `browser.request_takeover` records takeover reason, task activity, session state, and UI warning; `/browser-sessions/{id}/hand-back` records user summary and releases the session. Proof: `tests/test_browser_operator_phase9.py::test_browser_session_manager_persists_state_takeover_and_revocation`, web build. | Browser UI, runtime pause/resume. | Takeover state, hand-back summary. | Backend takeover/hand-back/revoke proof plus UI route/build proof. |
| Authenticated browser sessions | User-consented saved sessions, revocation, per-task approval. | Implemented (Phase 9): sessions store consent purpose/allowed domains, storage-state reference, sensitive-site approvals, revocation timestamp, and close/revoke APIs; sensitive domains require explicit per-task approval before navigation. Raw cookies are not written to audit logs. Proof: `tests/test_browser_operator_phase9.py::test_browser_sensitive_navigation_requires_session_consent_and_approval`, governance tests, web build. | Browser profile manager, settings. | Session refs, consent, last used, revocation. | Session isolation/consent/revoke tests and audit/governance proof. |
| Cloud computer | Sandboxed terminal/files/browser/editor/package install/artifact export. | Implemented (Phase 10): broker-visible `computer.*` tools create durable cloud computer sessions, jail files under `/tmp/chronos_computers/{org}/{session}`, run audited resource-limited terminal commands with timeout handling, install packages through constrained pip/npm commands, return truthful degraded screenshots when no desktop runtime is attached, and export files/directories as durable artifacts. `/computer-sessions` API and `/computer` UI expose sessions, replay events, sandbox policy, and artifact/export history. Proof: `tests/test_computer_phase10.py`, `apps/web/e2e/computer-static.spec.ts`, `npx next build --webpack`. | Sandbox service, computer tools, UI. | `computer_sessions`, `computer_session_events`, sandbox filesystem, command logs, artifact exports, `/computer-sessions` API, `/computer` UI. | Build small app file in sandbox, run command, enforce timeout, export artifact, replay events. |
| Local computer bridge | Desktop bridge with authorized folders/apps/commands/local compute. | Implemented (Phase 10, API/runtime bridge): broker-visible `local_computer.*` tools create revocable authorized-folder grants, jail all local file access to the grant root, require human approval for local command execution and app-open requests, audit command/app events, and block revoked grants. The current API runtime records app-open requests truthfully as degraded until a packaged desktop bridge is attached. `/computer` UI lists local grants and supports grant/revoke. Proof: `tests/test_computer_phase10.py`, `apps/web/e2e/computer-static.spec.ts`, `npx next build --webpack`. | Desktop app/bridge, broker, policy. | `local_computer_grants`, `local_computer_events`, folder grants, command audit, approval-gated local command/app tools, `/computer-sessions/local-grants` API, `/computer` UI. | Unauthorized folder escape blocked; unapproved local command rejected; approved command runs in granted folder; revoke blocks reuse. |
| Long-running delegated work | Manus-style task console with plan, live actions, artifacts, pause/resume/cancel. | Partial task/activity UI exists; complete console missing. | Tasks UI, runtime trace. | Durable task timeline and controls. | Long task monitored, paused, resumed, completed after refresh. |

## Coding Agent

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Repo workspace | Clone/import repos, create branch, inspect/edit/test/diff. | Implemented (Phase 11): broker-visible `repo.*` task-workspace tools now import local workspace repos or public GitHub HTTPS clone URLs, initialize/git-track workspaces, create branches, list/read/write jailed repo files, run constrained pytest commands, report status, and return git diffs. Coding work is handled from chat through the `coding` chat mode instead of a separate `/coding` surface. Live private GitHub clone depends on connector credentials and remains policy/credential-bound. | Coding runtime, GitHub connector, cloud/local computer, chat runtime. | Repo workspace path, branch, source metadata, file tree, test stdout/stderr/status, git status, diff, chat mode/tool traces. | `apps/api/tests/test_repo_workspace.py` and `apps/web/e2e/coding-agent-static.spec.ts`: import fixture/local repo, branch, inspect/edit, red/green test loop, diff/status, chat-first route guard. |
| Commit/PR flow | Commit and open PR under approval/policy. | Implemented (Phase 11, governed local PR request): `repo.commit` stages workspace changes and returns commit SHA/message; `repo.create_pr` returns `approval_required` without approval evidence and records `.chronos/pull_request.json` plus a `chronos://repo-pr/...` URL after `approval_id` is supplied. Live GitHub PR publication remains credential-dependent and must build on this approved request artifact rather than bypassing policy. | GitHub connector, broker, approvals, repo workspace. | Commit SHA, PR request URL, approval id, PR metadata artifact. | `apps/api/tests/test_repo_workspace.py::test_repo_workspace_import_list_status_commit_pr_and_review` proves commit SHA plus approval-gated PR request artifact. |
| Code review | Review PR/issues with inline findings and suggested patches. | Implemented (Phase 11 local review artifact): `repo.review` reviews the current diff/workspace, emits inline findings with file/line/severity/body/suggested_patch, summarizes changed files, and persists `.chronos/code_review.json`; chat/tool traces expose the review result. Remote PR comment publishing remains credential-dependent. | Coding agent, GitHub connector, chat UI. | Review artifact with inline findings and suggested patches. | `apps/api/tests/test_repo_workspace.py::test_repo_workspace_import_list_status_commit_pr_and_review` plus `apps/web/e2e/coding-agent-static.spec.ts`. |
| Test/debug loop | Run tests, inspect failures, patch, rerun, summarize. | Implemented (Phase 11): `repo.run_tests` accepts only safe pytest commands, rejects shell operators/path escapes, captures command/status/stdout/stderr/returncode/truncation, and supports red→patch→green loops with diff/status/review/commit follow-through. | Computer/coding tools, repo workspace, chat UI. | Test command status/stdout/stderr, diff, status, review/commit artifacts. | `apps/api/tests/test_repo_workspace.py`: failing fixture/local tests, patch, green rerun, rejected shell command, path-escape rejection. |

## Scheduled Work, Workflows, and Agents

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Scheduled tasks | One-time/daily/weekly/monthly/interval/event-triggered tasks. | Implemented (Phase 12): schedule API now supports one-time, daily, weekly, monthly, interval, webhook, and connector-trigger schedules, including paused/active state, next-run calculation, manual run, APScheduler polling, task materialization through the existing task runtime, run history rows, and audit events. Proof: `apps/api/tests/test_scheduled_tasks.py` plus Workflows UI static guard. | Scheduler, tasks UI/API. | `scheduled_tasks`, `scheduled_task_runs`, timezone-normalized next run, trigger metadata, `/schedules` API, `/workflows` UI schedule panel. | Schedule kinds compute correctly, due schedules spawn real tasks, paused schedules skip, run history is recorded. **Done** via `pytest apps/api/tests/test_scheduled_tasks.py` and `apps/web/e2e/workflows-static.spec.ts`. |
| Workflows | Convert tasks to reusable workflows with steps/dependencies/conditions/approvals. | Implemented (Phase 12): workflow definitions accept reusable connector steps, dependencies, conditions, retry metadata, and trigger definitions; workflow run APIs expose manual start, event dispatch, pause/resume/cancel, recovery, run detail, and lifecycle history. Existing chat convert-to-workflow remains wired to the same runtime. Proof: `apps/api/tests/test_phase12_workflows_monitors.py`, Workflows UI static guard. | Workflow runtime/UI. | `workflows`, `workflow_runs`, `workflow_steps`, `workflow_step_dependencies`, `workflow_state`, `workflow_triggers`, `workflow_run_events`, `/workflows` API/UI. | Create workflow, persist triggers, dispatch matching event, pause/resume/recover, inspect run history. **Done** via focused backend and static UI tests. |
| Monitors | Watch sites/sources/connectors/inbox/news and alert or run task. | Implemented (Phase 12 deterministic monitor layer): monitor configs cover website/source/connector/inbox/news/digest target types, status, conditions, evidence, evaluation endpoint, cited alert persistence, pause/resume, audit events, and `/workflows` UI panels for monitors and alerts. Live provider polling is intentionally delegated to schedules/connectors; deterministic evaluation is the product seam. Proof: `apps/api/tests/test_phase12_workflows_monitors.py`, Workflows UI static guard. | Monitor service, scheduler, connectors. | `monitors`, `monitor_alerts`, schedule/workflow refs, evidence JSON, `/monitors` API, `/workflows` UI. | Fixture change creates cited monitor alert with evidence URL/snippet and paused monitors do not evaluate. **Done** via focused backend and static UI tests. |
| Agent profiles | Reusable agents/personas with instructions/tools/projects/memory/autonomy/policy. | Implemented (Phase 13): durable `agent_profiles` store role, instructions, model, tool grants, connector grants, project bindings, memory scopes, autonomy level, approval policy, schedule permissions, lifecycle status, audit, and profile events. `/agents/templates` exposes research, executive assistant, sales/SDR, support, engineering, data analysis, and operations templates. `/agents` UI is now a real agent builder/run/publish workspace instead of an empty panel. Agent runs create normal governed Chronos tasks with the profile embedded in task state and project/memory policy preserved. Proof: `apps/api/tests/test_agents_phase13.py`, `apps/web/e2e/agents-static.spec.ts`, `npm run build -- --webpack`. | Agents API/UI, settings, runtime. | `agent_profiles`, `agent_profile_events`, profile CRUD, run endpoint, tool grants, connector grants, project bindings, memory scopes, approval/schedule policy, task state profile snapshot. | Create agent, attach project/tool/memory scope, run constrained task, cross-org read blocked, UI route/build proof. **Done** via tests above. |
| Agent publishing | Publish agents to Slack/Teams/email/web/API. | Implemented (Phase 13): `agent_publications` supports Slack, Teams, email, web, and API targets with display/channel mapping, per-publication inbound token, approval policy inherited from the agent, profile events, and audit rows. The inbound fixture endpoint validates the token, preserves external conversation/message metadata, creates a normal Chronos task linked to the publication, and embeds publication/profile state for runtime policy. Live delivery into real Slack/Teams/email providers remains credential-dependent and rides the Phase 8 connector action framework. Proof: `apps/api/tests/test_agents_phase13.py`, `apps/web/e2e/agents-static.spec.ts`, `npm run build -- --webpack`. | Connectors, webhooks, agent runtime. | `agent_publications`, inbound token, target mapping, conversation/task linkage, audit/profile events. | External fixture message creates Chronos task and audited publication/task chain; all required publish targets accepted. **Done for governed mapping/inbound bridge; live provider delivery credential-dependent.** |

## Collaboration, Admin, Mobile, and Compliance

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Shared conversations | Share chats with members/projects and role-based visibility. | Missing/partial unknown; not complete product. | Chat/projects/permissions. | Conversation ACL/share records. | Viewer can read but not mutate; unauthorized blocked. |
| Comments and mentions | Comments on projects/artifacts/tasks, mentions, assignments, handoff. | Missing. | Collaboration UI/API. | Comment, mention, assignment records. | Mention creates notification and access respects role. |
| Admin/RBAC | Members, roles, groups, workspaces, projects, agents, policies, retention. | Partial: settings/members + member role management exist; **member invitations implemented** (admin-only create/list/revoke, token never logged, accept-on-login provisions the member with the invited role, tenant-scoped, audited); groups/retention still missing. API keys, webhook dispatch, and ownership transfer remain truthful `_unsupported`. | Settings/admin, permissions, `core/invitations.py`. | `invitations` table; role/group/policy records. | `tests/test_invitations.py` (invite/list/revoke + accept/expiry/role); `tests/test_permissions_enforce.py` for role-gated access. |
| Audit/compliance export | Export audit, connector access, memory access, approvals, task reports. | Missing as productized export. | Admin/audit API. | Export jobs/artifacts. | Export contains expected events and no secrets. |
| Responsive mobile web | Core flows work on mobile viewports. | Unknown/incomplete. | Frontend. | Same APIs; responsive layouts. | Playwright mobile smoke for chat, approvals, task, artifacts. |
| Desktop app | Desktop shell for local bridge, notifications, folder auth. | Missing. | Desktop packaging/bridge. | Device registration and local grants. | Desktop bridge proof and revoke. |
| Notifications | In-app/email/Slack/Teams/desktop notifications for approvals/tasks/monitors. | Missing/partial. | Notification service, connectors. | Notification prefs, delivery records. | Approval notification creates actionable link; prefs honored. |
| Billing/plan controls | Enterprise plan/account controls if commercialized. | Missing; settings mention unsupported billing. | Admin/billing integration. | Plan/customer/subscription records if enabled. | Billing disabled is truthful or integration smoke passes. |

## Final Parity Proof Scenarios

| Scenario | Required flow | Required proof |
|---|---|---|
| ChatGPT-style parity | Upload CSV and chart/image, analyze both, create cited answer, chart/report artifacts, regenerate/branch. | Playwright + API proof with persisted files, artifacts, branch lineage, and citations. |
| Claude-style parity | Create project, upload docs, ask project-grounded question, cite sources, create/edit/version artifact, save/inspect memory. | Project source citations, artifact version history, memory usage proof, refresh persistence. |
| Manus-style parity | Browser task navigates/clicks/types, pauses for takeover, resumes, downloads file, creates artifact, replays after refresh. | Browser session trace, takeover trace, artifact, and restart/refresh proof. |
| Enterprise governance | Risky write requires approval, unauthorized user blocked, authorized approval resumes exactly once, audit export proves chain. | Approval and audit records plus no duplicate external write. |
| Connector ecosystem | Connected source syncs, source is used in research, permission revocation removes access. | Sync state, citation, revoked access test. |
| Reliability and safety | Task survives API restart, cancellation stops work, prompt injection blocked, degraded connector reported honestly. | Restart/cancel/injection/fallback tests. |

## Completion Workflow

1. Pick a matrix row before implementing a feature.
2. Update the row status only after implementation and proof exist.
3. Add or update tests named for the row's acceptance proof.
4. Link proof artifacts or commands in the relevant PR/implementation summary.
5. Do not mark any row complete based on scaffolding, mocks, or UI-only controls.
