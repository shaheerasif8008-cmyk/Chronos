# Chronos Total Parity Matrix

## Purpose

This matrix is the controlling implementation contract for the Chronos Total Parity Program. It translates the north-star goal in `CHRONOS_TOTAL_PARITY_GOAL.md` into concrete, testable capability rows.

No parity claim is valid unless the matching row is marked implemented with proof. A row is complete only when it works through the real UI/API, persists durable state where applicable, respects tenant boundaries, emits audit evidence where applicable, survives refresh/restart where applicable, and has automated proof.

## Status Legend

- **Foundation present**: meaningful repo substrate exists, but the full product capability is not complete.
- **Partial**: user-facing or backend behavior exists for a subset of the target.
- **Missing**: no complete implementation found in the current checkout.
- **Planned**: deliberately listed as required parity work; no completion claim.

## Current Foundation Snapshot

As of 2026-07-16, the checkout contains substantial, tested implementations for
chat, governed execution, projects and source knowledge, memory controls,
artifacts, research, multimodal/data work, Browserbase browser sessions, E2B
cloud computers and repository workspaces, the macOS device bridge, workflows,
collaboration, compliance export, Stripe billing, Gmail delivery, and durable
notification delivery. That is repository proof, not a production certificate.

The product is still **not total-parity complete or GA-ready**. The controlling
blockers are:

- no current live AWS deployment, provider-account configuration, DNS/identity
  hardening, capacity/SLO evidence, backup restore rehearsal, or released-SHA
  end-to-end evidence;
- no exhaustive Computer Use review of every desktop/mobile route and state on
  the release candidate;
- incomplete product/proof capabilities: the accessibility row's
  axe/VoiceOver/keyboard/zoom/contrast evidence;
- repository implementations that still require live provider proof before
  launch: Browserbase navigation/takeover/download/restart, E2B desktop and repo
  resume, Gmail search/draft/send, connector sync/revocation, Stripe checkout/
  portal/webhooks, SendGrid delivery, multimodal generation, and independent
  model fallback; and
- final ChatGPT-style, Claude-style, Manus-style, enterprise-governance,
  connector, and restart/safety scenarios have not been executed together on
  one immutable deployed release.

The rows below separate implemented repository behavior from those outstanding
product and operational proofs. A static source-contract test or successful
build is never described as behavioral browser proof.

## Matrix Columns

- **Target parity**: what Chronos must provide.
- **Current state**: current repo/product state, without overstating readiness.
- **Implementation area**: subsystem that owns the work.
- **Interface and persistence**: public UX/API/state required.
- **Acceptance proof**: minimum proof before marking complete.

## Core Assistant and Chat

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| General chat assistant | ChatGPT/Claude-grade conversational assistant with streaming, history, model selection, citations, artifacts, memory refs, and tool traces. | Implemented at the repository and local-runtime seams: send → streamed governed model/tool loop → durable assistant persistence → refresh/reload, with model/mode, citations, tool traces, memory refs, artifacts, structured response, and status normalized back into the UI. Conversations are immutably bound to an authorized native workspace. | Chat UI, chat router, LLM layer, message model. | Conversation/message records include workspace, model, mode, citations, tool traces, memory refs, artifacts, structured response, and status. | `apps/web/e2e/chat.spec.ts`, backend chat/message suites, and `apps/web/e2e/client-readiness-static.spec.ts`. Repeat the behavioral flow against the final deployed release before launch. |
| Model selection | User can choose models/modes by task with safe fallback and clear provider status. | Implemented for selection, persistence, and real failure-domain separation. `/chat/models` exposes concrete IDs, labels, capabilities, availability, tool-use, fallback role, and policy; the composer persists the choice. Primary OpenRouter calls now fail over to a separately keyed direct-provider model for agent, tool, structured, text, vision, and pre-output streaming failures. Production startup and Terraform reject a missing backup or an `openrouter/*` backup model. Streams are never replayed after visible output, preventing duplicate actions/text. | LLM config, chat composer, settings. | Model registry plus direct-provider backup model/key and provider-status metadata. | `tests/test_llm_and_memory.py`, `tests/test_streaming_step.py`, and production-config validation prove routing and failure injection. A live OpenRouter outage/direct-Anthropic success exercise on the deployed account remains the release proof. |
| Reasoning and tool mode control | User can select default, research, agent, browser, computer, data, image, voice, or coding modes. | Implemented for current mode surfaces: centralized mode registry, `/chat/modes`, persisted composer selection, status/capability metadata, and normalized mode persistence on chat/task records. Unavailable modes are explicitly labeled unavailable instead of silently pretending capability exists. Proof: tests/test_chat_modes.py, `npx next build --webpack`. | Chat composer, router, runtime planner. | Message/task mode persisted and shown in UI. | Backend mode registry/persistence tests + web build. |
| Chat controls | Edit prompt, regenerate, branch, retry from here, pin, copy, export, save to memory, convert to task/workflow. | Implemented for current message controls: pin/unpin, copy, edit user message, branch, save-to-memory, convert-to-task, convert-to-workflow, export, regenerate assistant response, and retry-from-here now have UI actions and backend/API support where persistence is required. Proof: tests/test_chat_controls.py, apps/web/e2e/chat-controls-static.spec.ts, `npx next build --webpack`. | Chat UI, message actions, task/workflow APIs. | Message action endpoints and branch/fork lineage. | Backend chat-control tests + static UI route/action guard. |
| Conversation search | Search across conversations, tasks, artifacts, memory, and sources. | Implemented as a global federated palette. `GET /search` covers conversations, messages, tasks, artifacts, every memory scope readable through the canonical control-center ACL, project-source titles, and indexed source/connector chunk content. Per-type tenant/member/project/document ACLs, private/revoked-source filtering, literal LIKE escaping, ranking, limits, and hashed (not raw) query audit evidence are enforced. The app-wide Cmd/Ctrl+K shortcut and sidebar entry open the keyboard-accessible grouped palette from every product section and route each result to its owning surface. | Search service, UI global search. | Permission-filtered persisted entity queries; source chunks and memory use their canonical read predicates. | `tests/test_search.py` proves org isolation, all-scope memory ACL use, chunk-only authorized source hits, private/revoked exclusion, filters/ranking/limits/degrade behavior; `apps/web/e2e/knowledge-projects-static.spec.ts` proves the app-wide six-type palette wiring. |

## Runtime, Agent, and Reliability

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Native tool loop | Model-native decide-act-observe loop with parallel tool calls and honest failure handling. | Implemented (Phase 1): runtime agent loop with persisted iteration history, tool calls/results/errors, injection-gated writes, and cancellation checks. Proof: tests/test_inline_tools.py and tests/test_runtime_reliability_phase1.py::test_run_loop_gates_write_after_untrusted_prompt_injection, ::test_run_loop_stops_before_model_step_when_task_is_cancelled. | Runtime agent loop, tool registry, broker. | Persisted agent history, iteration count, tool calls/results/errors. | Multi-step loop, tool error handling, injection gate (tests above). |
| DAG execution | Complex task DAG with dependencies, conditions, parallel execution, checkpoints, and replanning. | Implemented (Phase 1): executor/planner with step state, conditional skip, checkpoints, and resume. Proof: tests/test_orchestration_category1.py, tests/test_state_management_category5.py. | Runtime executor, planner, task state. | Task plan, completed/skipped steps, result context, checkpoint records. | Conditional skip, step state, resume (tests above). |
| Task queue | Priority queue, per-org/user/tool concurrency, worker limits, dead-letter state. | Implemented for the current multi-replica runtime: the Postgres-backed task state, Redis leases/heartbeats, priority runner, concurrency limits, startup recovery/reaper, retry taxonomy, dead-letter flag, and explicit retry API are present. The former single-process/non-distributed language in `CLAUDE.md` is historical and no longer controls production architecture. | Runtime task runner, Redis coordination, Postgres queue state. | Queue records, priority, attempts, lease/heartbeat, failure taxonomy, dead-letter flag. | `tests/test_runtime_reliability_phase1.py`, `tests/test_state_management_category5.py`, and multi-replica/live restart evidence before launch. Repository tests are present; production failure-injection proof remains outstanding. |
| Cancellation | User can cancel running tasks and tool work stops promptly. | Implemented in the repository across long-lived runtimes. Task cancellation atomically marks the tenant-scoped task subtree, persists one unique cleanup request, signals local runners and sub-agents, and uses expiring claims, backoff, startup recovery, and a leader reaper to reconcile provider failures. Idempotent cleaners close only task-bound Browserbase sessions, E2B computer sessions, E2B repo workspaces, virtual desktops, paired-device commands/grants, connector jobs, approvals, and task leases. Running connector adapters receive cooperative cancellation; provider errors are redacted before persistence/audit. `GET /tasks/{id}/cleanup` exposes durable progress. | Runtime cancellation coordinator, task runner, provider session managers, connector framework, leader jobs, Tasks API/UI controls. | `task_cleanup_requests`, tenant/task subtree binding, claim/retry state, redacted per-resource summary, completion/audit/activity evidence. | `tests/test_task_cancellation_cleanup.py` proves duplicate cancellation, cross-tenant rejection, restart/expired-claim recovery, sanitized provider retry, running adapter cancellation, and sibling-session isolation; adjacent runtime/computer/repo/desktop/connector suites remain green. Real provider termination under a deployed multi-replica release remains launch proof. |
| Timeouts and retries | Step-level and task-level timeouts, retry policy, final failure taxonomy. | Implemented (Phase 1): settings-backed max attempts + task timeout in the runner; **final failure taxonomy** (`timeout`/`cancelled`/`error`) persisted on the task with attempt count. Connector-specific retry policies continue under the connector framework. Proof: tests/test_runtime_reliability_phase1.py::test_task_runner_retries_failed_task_before_marking_failed, ::test_timeout_dead_letters_with_timeout_taxonomy, ::test_classify_failure_taxonomy. | Runtime runner, settings. | Per-task policy, retry count, failure_reason. | Timeout, retry-then-fail, taxonomy classification (tests above). |
| Crash recovery | Recover after restart for every task/session type that exists. | Implemented for current durable work: startup re-enqueues incomplete tasks, recovers workflows and research runs, resumes approval decisions once, and the elected leader now recovers pending/processing project-source uploads, URLs, connector feeds, and child documents. Source recovery is bounded per pass and idempotent; connector feeds revalidate their owner, project membership, and credential before refetching. Browser/computer/desktop recovery is tracked by their own session/device controllers. | Startup recovery, task runner, source indexing. | Durable task/run/source state sufficient for resume or safe fail. | Existing runtime recovery proofs plus `tests/test_source_indexing.py::test_restart_recovery_routes_uploads_and_connector_feeds`. |
| Durable trace replay | Full task timeline replayable after refresh/restart. | Implemented (Phase 1): replay-significant activity events (tool calls/results, approvals, artifacts, sub-agents, task completion/failure/cancel incl. dead-letter, route/model steps) persist to append-only audit and replay after restart. High-frequency heartbeat stays Redis-only **by design** (durable trace carries replay-significant events, not per-tick liveness). Proof: tests/test_runtime_reliability_phase1.py::test_publish_activity_persists_replayable_model_trace **and E2E** apps/web/e2e/activity.spec.ts (create task → complete → open Activity timeline → refresh twice → persisted event count matches). | Trace persistence, activity API, UI timeline. | Durable events for model, tools, artifacts, approvals, completion. | Replayable trace persisted (backend test) + refresh-stable timeline (E2E activity.spec.ts). |
| Idempotent writes | External write actions execute at most once across retries/recovery. | Implemented for every current mutation seam. A Postgres connector-write ledger/outbox binds tenant, member, task, tool, payload, approval, and idempotency evidence before dispatch; uses atomic claim leases; encrypts recoverable outbox payloads; commits redacted provider responses before any later await; and adopts known success after a crash. Providers with a real idempotency primitive reuse one opaque provider key; providers without idempotency/reconciliation stop in durable `manual_review` after an ambiguous outcome and are never blindly retried. Redis-loss recovery rebuilds framework jobs from Postgres. Coverage includes framework/custom HTTP, direct generic HTTP and named OAuth catalog writes, Composio, MCP, and nested platform calls. Gmail send, GitHub PR publication, billing Stripe, webhooks, browser, computer, and local-device mutations retain their stronger specialized state machines without double-wrapping. | Tool broker, connector framework/worker, durable ledger/outbox/reaper, generic/custom HTTP, Composio/MCP/platform, specialized delivery adapters. | `connector_write_operations`, encrypted outbox, immutable binding hashes, provider key/capability, claim/retry/manual-review state, redacted result/evidence and retention. | `tests/test_connector_write_ledger.py` plus connector/Gmail/billing, broker/tenant/cancellation regressions prove crash-after-response adoption, immediate exception ambiguity, Redis-loss and ledger-before-job recovery, cross-tenant/key/payload conflicts, same-key provider retry, and manual-review fail-safe. Live provider replay/reconciliation remains launch evidence. |

## Governance, Safety, and Audit

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Permission seam | Every action uses `permission.check`; future policy engine can replace implementation without call-site changes. | Foundation present. | Permissions core, routers, runtime. | Audit of permission decisions and tenant/resource context. | Static grep plus tests for protected routes/tools. |
| Tool broker seam | Every external action routes through `tool_broker.execute`. | Foundation present; W3 added centralized connector-rate governance and loop-triggered org suspension at the broker boundary. | Broker, tool registry, connectors. | Tool call/result audit with redacted args. | Static grep plus tool execution tests; W3 governance tests for rate/cost admission. |
| Approval system | Risky writes pause, route to authorized approvers, resume exactly once, and reject cleanly. | Implemented at the product seam. Approval decisions have an absolute role floor (`owner`, `admin`, or `approver`) independent of OpenFGA; tenant-scoped HTTP mutation, persisted actor/decision, seeded inbox UI, and resume are covered. Gmail send additionally binds the approved payload/member/task and stores durable provider delivery evidence before sending. | Approvals router/UI, permission seam, broker policy, connector delivery adapters. | Approval record, exact approved payload, status, actor, resume and provider-delivery evidence. | `tests/test_approval_flow_http.py` proves authorized decide/resume and unauthorized 403/no resume; `apps/web/e2e/approvals.spec.ts` proves the seeded inbox path; `tests/test_gmail_send_delivery.py` proves approval-bound retry/recovery. A deployed multi-replica exactly-once write remains a launch gate. |
| Audit log | Searchable/exportable audit for tool, memory, approval, policy, connector, task, and admin events. | Implemented for the current product surfaces: append-only tenant-scoped audit search plus CSV/JSON export, nested credential redaction at the append boundary, and a durable tamper-evident compliance JSON artifact covering audit, connector access, memory access, approvals, and task execution. | Audit service, compliance export, admin UI. | Append-only redacted rows, filtered exports, hash-chained signed manifest artifact. | `tests/test_compliance_export.py`, audit endpoint tests, and `apps/web/e2e/compliance-export-static.spec.ts`. The latter is source-contract proof; an authenticated download from the deployed release remains required. |
| Prompt injection defense | Untrusted browser/file/email/connector content cannot issue instructions or trigger actions. | Implemented at the shipped ingestion/runtime seams: uploads, parsed files, URLs, connector documents, browser results, and OCR-derived text are treated as external evidence; known source injection patterns quarantine the source and remove its chunks. Retrieved excerpts are escaped inside explicit `<untrusted_source>` boundaries and never presented as instructions. Any project-source evidence in task history forces external writes back through approval even when no known phrase was detected. | Content ingestion, LLM context assembler, broker policy. | Untrusted wrappers, scan/quarantine state, citation provenance, approval grounding. | `tests/test_source_indexing.py`, `tests/test_source_retrieval.py`, `tests/test_runtime_reliability_phase1.py::test_project_source_evidence_gates_external_write_even_without_known_phrase`. |
| Tenant isolation | Every API/query/tool/artifact/memory path enforces organization and permission scope. | Proven at multiple layers: search org-isolation (test_search.py), in-process artifact cross-org (test_artifact_workspace.py), HTTP-boundary cross-org 404 over the real ASGI app with real auth/tokens (test_tenant_isolation_http.py), and W3 tenant-bound vault reads by `(organization_id, vault_ref)`. Broader per-resource HTTP coverage (tasks/connectors/projects) still expanding. | DB queries, routers, broker, storage, vault. | Org/region on every persisted row and storage/cache path. | Cross-tenant tests for memory, artifacts, tasks, connectors, projects. **Artifacts proven over HTTP** via test_tenant_isolation_http.py; vault boundary covered by test_w3_governance.py. |
| Secret handling | Credentials encrypted, never logged, revocable, and redacted in traces. | W3 hardened the vault read seam: `vault.get` requires `org_id`, cache keys are tenant-qualified, durable lookup filters by organization before decrypting, and Gmail/generic OAuth/framework credential loaders pass tenant context. Raw connector args/results remain redacted in broker audit. | Vault, connectors, audit redaction. | Vault refs only in logs; rotation/revoke state; tenant-bound credential cache. | Redaction test suite and connector revoke proof; W3 vault tenant-bound proof in test_w3_governance.py. |
| Degraded-mode honesty | Fixture/demo/fallback data is never presented as live. | Substantially implemented: production startup rejects demo mode; browser/search, research, E2B execution, notifications, billing, multimodal providers, and connector health expose unavailable/degraded reasons rather than fabricated success. Coverage is broad but not a proof that every provider error shape is translated correctly. | Connector framework, execution runtimes, provider health, UI status. | Result metadata includes status/provider/tier/fallback reason; unavailable capabilities disable or explain their controls. | Provider/config/degraded tests plus `apps/web/e2e/connector-health-static.spec.ts`; controlled live quota/outage exercises remain required. |
| Browser/computer file and egress safety | Isolated execution, network controls, malware/download scanning, file restrictions, and egress review. | Implemented in the repository with live-provider evidence outstanding. User attachments, Browserbase downloads/task-workspace uploads, and base64 connector-synchronized binaries cross the required private ClamAV boundary before persistence or browser handoff. Infected bytes and unsafe replacements are discarded/quarantined, scanner errors fail closed, and active content (executables, macro/embedded/external Office content, active/encrypted PDFs, active HTML/SVG, unsafe archives) is rejected. Clean connector binaries are parsed into a newly generated text-only artifact; raw provider bytes are never persisted. `file_security_events` stores tenant-scoped metadata-only scan/disarm evidence and drives an admin-only quarantine queue whose acknowledge/close/explicit false-positive decisions cannot restore bytes. Runtime health/onboarding require the scanner, and ECS gates the API on its sidecar. Code/data and offline computer profiles use explicit dual-stack deny-all plus a destructive pre-use socket attestation. Network-enabled computer sessions require a human-approved exact-domain subset of `E2B_COMPUTER_EGRESS_ALLOWLIST`; the repo profile uses `E2B_REPO_EGRESS_ALLOWLIST`. E2B receives `allow_out` plus dual-stack deny-all, private public-traffic settings, a hashed policy marker, and a destructive pre-use probe proving one allowed TLS destination succeeds while unlisted `1.1.1.1:443` remains blocked. | ClamAV sidecar, bounded content disarm, connector sync, operator quarantine review, Browserbase/E2B profiles, runtime health, security operations. | Artifact scan fields, metadata-only `file_security_events` review state, text-only connector artifacts, browser download scan metadata, consented domain subset, provider policy marker and pre-use attestation. | `tests/test_file_security.py` covers clamd protocol, clean/infected/error behavior, ingress rejection, active-content inspection, text-only disarm, and metadata-only review; `tests/test_source_sync.py` proves connector binaries are scanned and only sanitized text persists; `tests/test_production_execution_boundary.py`, `tests/test_e2b_egress_policy.py`, and `tests/test_computer_phase10.py` prove deny/allowlist construction, leaky-sandbox destruction, policy validation, and consent binding. Focused regressions and Terraform validation are green. Remaining launch evidence is a credentialed E2B allowed/blocked attestation for computer and repo templates plus a real Browserbase clean/malicious download/upload exercise against the deployed release. |

## Projects and Knowledge Sources

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Projects | Claude/ChatGPT-style projects with instructions, members, conversations, sources, memory, tasks, artifacts, and tools. | Implemented as a first-class API/UI surface: create/list/open projects; owner/member management with last-owner protection; linked conversations/tasks/artifacts/sources/research/comments; project-chat creation; source management; settings and instruction history. Owners choose private or organization-visible access. Organization visibility grants same-tenant read-only access while explicit membership remains required for edit/manage rights, and the OpenFGA model mirrors the organization-member view relation. A non-empty `default_tools` list is a validated project allowlist (empty inherits workspace policy): it filters chat/durable-task tool schemas and is rechecked at the broker before execution so stale/resumed calls fail closed. | Projects API/UI, canonical project access, OpenFGA, connector tool resolution, broker, context assembly. | `projects.visibility`, `projects.default_tools`, project membership/role tuples, immutable instruction versions, linked records, task project identity. | `tests/test_projects.py`, `tests/test_project_instructions.py`, authz tests, and `apps/web/e2e/knowledge-projects-static.spec.ts` prove visibility isolation, owner-only mutation, bounded tool policy, broker enforcement, settings UI, and existing project workflows. Deployed two-member private/organization and mid-task policy-change proof remains release evidence. |
| Project instructions | Project-level instruction layer merged safely with system/user context. | Implemented. Every edit increments a locked version counter, appends immutable `project_instruction_versions`, and emits audit evidence. Chat and durable project tasks assemble instructions, scoped memory, connectors/skills, and permission-filtered source citations from durable project identity. Resume rebuilds that context so revoked membership or source/memory ACL changes cannot reuse a cached project prompt; non-conversation agent triggers are never treated as conversation UUIDs. | Context assembler, projects, task runtime. | Versioned instruction history and audited edits. | `tests/test_project_instructions.py`, including project isolation, task propagation, resume revocation, and agent-trigger UUID safety. |
| Source upload | Upload PDFs, docs, slides, sheets, text, code, images, URLs. | Implemented for the shipped general workflow. Project Sources directly accepts multi-file uploads, code folders/ZIPs, and broker-fetched URLs; the parser handles PDF, Office documents/slides/sheets, CSV/text/Markdown/code, safe ZIP traversal, and image OCR. Raw and parsed artifacts are durable, parse/index states remain visible, empty/unparseable inputs fail honestly, production object-store failure creates no ephemeral artifact/source, and pending work is restart-recoverable. | Upload service, durable storage, parser/indexing pipeline, Project Sources UI. | Source record, durable object path, parse state, extracted child artifact, chunks. | `tests/test_project_sources.py`, `tests/test_source_indexing.py`, parser tests, `tests/test_artifact_workspace.py::test_production_storage_failure_creates_no_ephemeral_artifact`, web static proof. |
| Source indexing | Chunk, embed, refresh, remove, reindex, cite, permission-resync sources. | Implemented with bounded production behavior: parse, external-content scan/quarantine, chunk/embed, refresh/reindex/delete, permission-filtered vector retrieval, stable citations, and removal/revocation chunk deletion. Indexing is transactionally all-or-nothing, capped at 512 chunks per source with a visible partial-index warning, and recovered from durable pending state after restart. Per-document private/allow/deny/revoked ACLs are applied consistently in list/detail/search/download/retrieval. | Ingestion pipeline, embeddings, retrieval, source APIs/UI. | Source chunks/embeddings, trust metadata, permissions, freshness and recovery state. | `tests/test_source_indexing.py`, `tests/test_source_retrieval.py`, `tests/test_source_viewer.py`, and chunk-only ACL search proof in `tests/test_search.py`. |
| Connector-synced knowledge | Drive/SharePoint/Notion/GitHub/Slack/Gmail/Teams indexed as project/org sources. | Implemented through the connector framework for connected providers. Feeds bind to a caller-visible active credential, fetch only through `tool_broker.execute`, normalize common provider result shapes, persist/index child documents under a durable parent feed, and remove documents that disappear upstream. Every sync/recovery revalidates the active feed owner, current project membership, connector ownership/shared status, provider/tool binding, and per-document ACL; revoked access deletes chunks and surfaces a revoked/failed state rather than fixtures. Provider breadth still depends on which real accounts are configured. | Connector sync jobs, broker, retrieval, projects, research. | Parent feed/document relationship, sync state, permissions, chunk index, revoked/failure evidence. | `tests/test_source_sync.py` (owner/credential lifecycle, stale removal, normalization, failure/revoke) plus connector-citation research tests. |
| Source viewer | Inspect original, extracted text, chunks, warnings, index status. | Implemented in Project Sources. Authorized members can inspect metadata, parse/index/quarantine/revocation/truncation warnings, and paginated extracted chunks; owners can reindex/delete and connector feeds can sync. Original bytes download through a dedicated source ACL endpoint, so the UI never exposes or widens the underlying private artifact id. Deep links from global search open the exact source. | Project source UI, artifacts/storage. | Redacted source DTO, ACL-scoped content endpoint, paginated chunk preview. | `tests/test_source_viewer.py`, `apps/web/e2e/knowledge-projects-static.spec.ts`, production web build. |

## Memory

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Scoped retrieval | Authorized multi-signal memory retrieval with query expansion, ranking, dedupe. | Implemented across org, canonical workspace, personal/restricted, project, persona, task scratchpad, and conversation scopes. Every payload-provided resource id is ACL-validated before it becomes a scope; query expansion, semantic ranking, recency/importance/authority signals, pin boost, near-duplicate suppression, usage provenance, disabled-memory policy, archive/supersession filtering, and sensitive-memory exclusion from automatic model context are enforced. Global search separately uses the same all-readable-scope SQL predicate so authorized project/conversation/task/persona memory is discoverable without widening model context. | Memory core, embeddings, requester context, global search. | Scoped memory rows, provenance/usage records, validated resource pairs. | `tests/test_memory_context_privacy.py`, `tests/test_memory_parity.py` (including sensitive exclusion and conversation policy), `tests/test_llm_and_memory.py`, `tests/test_search.py`. |
| Explicit memory | User says remember/forget/edit and memory updates immediately. | E2E-proven: add (remember) → retrieve (list) → edit → delete through the real memory UI, stable across reruns. Proof: apps/web/e2e/memory.spec.ts. | Chat route, memory router/UI. | Memory entry and audit event. | E2E remember, retrieve, edit, delete. **Done** via apps/web/e2e/memory.spec.ts. |
| Autonomous memory | Assistant extracts useful durable facts visibly with undo. | Implemented for current chat capture: policy is checked before model extraction and again at the durable write; useful facts are persisted as member-personal memory only (never silently promoted to shared project/org context), publish on an owner-specific conversation channel, and appear in the composer as a dismissible “Saved to memory” notice with a 60-second Undo action. Undo is owner-only, autonomous-source-only, single-use, database-clock bounded, audited, and legal-hold aware; the durable memory remains visible in the control center even if a transient live notification is missed. | Extraction job, durable memory write, member-scoped events, chat UI. | Source conversation, personal scope, undo window/event/audit. | `tests/test_memory_parity.py::test_autonomous_memory_undo_is_owner_only_single_use_and_time_bounded`, privacy/event tests in `tests/test_memory_context_privacy.py`, `apps/web/e2e/knowledge-projects-static.spec.ts`. |
| Memory control center | Search/edit/delete/archive/merge/pin/scope/import/export, usage logs. | Implemented in API and UI: search/filter, create/edit/delete, archive, pin, sensitive classification, conflict review/resolution, explicit duplicate merge with confirmation, scope change, import/export, capture policy, and per-memory usage history are wired to persisted endpoints. The server revalidates every scope transition and import item. | Memory UI/API, import/export. | Memory status, provenance, policy, conflict/supersession, usage records. | `tests/test_memory_parity.py`, `tests/test_memory_context_privacy.py`, `apps/web/e2e/memory.spec.ts`, and `apps/web/e2e/client-readiness-static.spec.ts`. A full browser pass of every destructive/state variant remains part of release QA. |
| Memory conflict/staleness | Detect stale/conflicting memories and propose merge/replacement. | Implemented (Phase 4): deterministic near-duplicate detection (token Jaccard within scope) via `detect_conflicts`; `superseded_by` column marks stale entries, which are excluded from retrieval; `merge`/`resolve_conflict` supersede losers; conflicts review panel in UI. Proof: tests/test_memory_parity.py::test_conflict_detection_and_resolution, ::test_archived_and_superseded_excluded_from_retrieval. | Memory ranking, supersession. | Conflict links, stale flag, resolution history. | Newer preference supersedes and suppresses old conflicting one (test above). |
| Memory privacy controls | Disable memory per project/chat/user, classify sensitive memories. | Implemented across org/project/member/conversation policy on both write and retrieval. `RequesterContext` now carries conversation identity, which is ACL-validated for conversation-scoped recall and used for read-side disable policy plus usage provenance. The durable write primitive revalidates scope authorization; org/workspace writes require org admin, project sharing requires project owner/admin, and autonomous capture is always personal. Member-specific event channels prevent another conversation viewer from seeing private capture notices. Sensitive memories remain visible to their authorized owner in the control center/export but are excluded immediately from automatic model retrieval. | Settings, memory policy, retrieval/write/event seams. | Scoped policy settings, sensitive classification, usage provenance. | Privacy/control proofs in `tests/test_memory_context_privacy.py` and `tests/test_memory_parity.py`, including disabled write/retrieval, shared-scope governance, event isolation, and sensitive exclusion. |
| Per-message memory visibility | Show which memories were saved/used for a message, with immediate undo/edit controls. | Implemented in the repository. Autonomous capture publishes an owner-only “Saved to memory” notice with a 60-second durable undo boundary. Every assistant response persists the exact authorized retrieval references in `messages.memory_refs`; reload normalizes them into an expandable “Memory used” block showing content, scope, and provenance. Each reference deep-links to its exact control-center card and opens the editor, while normal memory ACLs revalidate the destination. | Chat message metadata, memory usage log, chat UI. | Message-to-memory usage/save references, durable chip state, owner-scoped event, deep-link edit target. | Backend chat/context privacy tests plus `apps/web/e2e/client-readiness-static.spec.ts` prove persistence/wiring. Complete a two-member behavioral browser refresh/deep-link negative-isolation exercise on the final release. |

## Artifacts

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Artifact creation | Assistant creates durable reusable outputs from chat/tasks. | Implemented (Phase 5): create via POST /artifacts and runtime save_artifact; version-addressed storage; list/download/duplicate; route boundary enforces tenant isolation (404 cross-org). Proof: tests/test_artifact_workspace.py::test_create_read_download_roundtrip_survives_refetch, ::test_duplicate_creates_independent_copy, ::test_router_blocks_cross_org_access. | Artifact core/router, runtime. | Artifact row, object storage/local fallback, message/task linkage. | E2E create artifact, refresh, download. |
| Artifact workspace | Side-by-side preview, full artifact browser, project/task grouping. | Implemented for current artifact types: searchable/filterable two-pane browser, project/task/conversation linkage, Preview/Edit/Versions, version timeline, diff/restore, authenticated download, and in-chat side panel. Query-string deep links and persisted versions survive refresh. | Artifacts UI/API. | Artifact collections, filters, links, version state. | `apps/web/e2e/artifacts.spec.ts` plus `tests/test_artifact_workspace.py`; complete Computer Use review of every artifact kind/state remains a release gate. |
| Artifact editing | User and AI can edit artifacts without destructive overwrite. | Implemented (Phase 5): non-destructive versioning (artifact_versions), manual edit, AI edit, restore, unified diff — all via permission seam + audit. Proof: tests/test_artifact_workspace.py::test_edit_creates_new_version_without_clobbering **plus UI E2E** apps/web/e2e/artifacts.spec.ts (create → preview → edit → new version → diff → restore through the real workspace; stable across repeated runs). | Artifact editor, version API. | `artifact_versions`, diff metadata. | Edit creates new version; restore works. **Done** (backend unit + UI E2E). |
| Artifact renderers | Markdown, HTML, React, SVG, diagrams, CSV/table, JSON, code, image, docs/sheets/slides. | Implemented for production-safe inspection: Markdown/GFM, code/text, JSON, bounded CSV/table, validated raster images, sandboxed sanitized HTML/SVG, structured DOCX/XLSX/PPTX, non-executing notebook cells/outputs, bounded ZIP manifests, and subprocess-rasterized PDF pages are wired through one authenticated/public preview contract. React and diagram artifacts are deliberately source-only rather than executing attacker-controlled code in the Chronos origin; binary formats remain immutable/downloadable rather than pretending to support lossless browser editing. | Renderer UI, parser bounds, credential-scrubbed media subprocesses, sandbox. | Renderer selection, one-million-character response budget, ZIP expansion/entry ceilings, PDF/image resource envelopes, authenticated fallback download. | `tests/test_rich_artifact_exports.py` covers every advertised safe renderer class, active-content removal, archive traversal, response/pixel ceilings, download headers, and PDF raster output; `apps/web` typecheck plus static UI contracts prove wiring. Fresh Computer Use review remains a release gate. |
| Artifact publish/share | Controlled share/publish/unpublish with permissions. | Implemented (Phase 5): governed publish/unpublish with signed-token public /shared/{token} link and revocation; seam-gated + audited. Proof: tests/test_artifact_workspace.py::test_public_share_boundary_serves_then_revokes. | Sharing API/UI, permissions. | Share links, ACL, publish status. | Unauthorized cannot access; unpublish revokes. |

> Phase 5 proof strategy: backend behavior is proven by DB-backed pytest against an isolated database. A Playwright behavioral E2E harness now exists in the repo (`apps/web/playwright.config.ts` + `apps/web/e2e/`, isolated dual-server with real dev-OTP auth); the artifact edit/version/diff/restore lifecycle is covered by `apps/web/e2e/artifacts.spec.ts`. Rows not yet covered by an E2E spec remain typecheck-verified via `npm run build`.
>
> Phase 5 lifecycle coverage: create, preview, edit, AI edit, version, diff, restore, rename, **duplicate**, **move**, delete (soft), publish/unpublish (revocable share link). **`move` is now implemented** (Projects landed in Phase 3): artifacts carry a direct `project_id` (migration 0023), the `POST /artifacts/{id}/move` endpoint re-homes them (tenant-scoped, audited), and the project artifacts query unions directly-moved artifacts. Proof: tests/test_artifact_workspace.py::test_move_artifact_into_project_and_unlink, ::test_move_rejects_cross_org_project_and_artifact.

## Deep Research

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Research mode | Dedicated quick/standard/exhaustive/trusted research runs. | Implemented in the repository: durable run/plan/status/findings/limitations/citations, source and citation policy, allowed/disallowed domains, time budget, cooperative cancellation, startup recovery, report viewer, and quick/standard/exhaustive/trusted modes. Prior local live-model/search runs are not evidence that the current production accounts or release are configured. | Research API/UI, planner, runtime. | Research run, plan, source scope, domain/citation policy, depth, status. | `tests/test_deep_research.py` and behavioral `apps/web/e2e/research.spec.ts`. Re-run the real model/search flow on the deployed SHA and preserve report/trace evidence before launch. |
| Citation collector | Store source snippets, metadata, confidence, limitations. | Implemented: citation rows carry marker, source type/id/title/URL, non-empty snippet, confidence/distance, and untrusted-content metadata; degraded/unavailable sources create limitations instead of citations. | Retrieval/browser/research. | Citation records linked to report/message/artifact. | `tests/test_deep_research.py::test_add_citation_requires_snippet` and tenant/source-scope tests prove repository behavior. Re-run a multi-source cited report on the deployed release; no current production live-run claim is made here. |
| Internal + external research | Merge project files, web, connector sources, uploads, MCP. | Implemented at the repository seam for web, project/indexed connector documents, indexed uploaded project files, and explicitly selected read-only MCP tools. MCP discovery excludes ambiguous/write tools; returned evidence is treated as untrusted, stored as real citations, and unavailable scopes produce limitations instead of fabricated citations. | Research planner, retrieval, connectors/MCP. | Source scope, selected MCP tool/arguments, citation and retrieval provenance. | `tests/test_deep_research.py::test_internal_external_merge`, `::test_connector_indexed_sources_are_used_as_research_citations`, `::test_research_mcp_discovery_exposes_only_declared_read_tools`, and `::test_uploaded_and_read_only_mcp_sources_create_real_citations`. Live connected-source/MCP proof is outstanding. |
| Research report artifact | Export report to artifact/document/PDF with source table. | Implemented. Completed Markdown reports export from the Research UI to immutable DOCX or PDF child artifacts with canonical limitations and citation/evidence appendices, are linked back to the project, reopen from Artifacts, and download immediately. A Postgres session advisory lock plus a post-lock recheck makes each report/format exactly-once across API replicas and avoids duplicate renderer work during concurrent clicks. Project artifact tabs also create durable bounded ZIP bundles with a checksum manifest and explicitly project-shared artifacts only. | Research UI, artifact/project routers, DOCX/PDF/ZIP generation. | Parent/child artifact lineage, project linkage, source appendix, export derivative, checksum manifest. | `tests/test_rich_artifact_exports.py` proves DOCX/PDF contents and visual-raster validity, real-Postgres concurrent exactly-once export, safe ZIP manifest/names, limits, and project scope; `apps/web/e2e/research-static.spec.ts` and `project-research-static.spec.ts` prove UI/API/download wiring. Live deployed download/reopen is still a release gate. |

## Multimodal, Files, and Data

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| General file upload | Attach files to chat/project/task/research/artifact. | Implemented (Phase 7): `/attachments` uploads → `save_artifact(kind="attachment")` with object path, links to conversation/project/**task**/**research run** (tenant-scoped, cross-org 404, audited); uploaded `attachment_ids` persist onto the user message (`messages.artifact_refs`) and round-trip through history so attachment chips reappear after refresh; composer surfaces upload failures. Proof: tests/test_file_upload.py (persist-after-refresh, task link+audit, cross-org 404 for conversation/task/research), `npm run build`. | Upload service, storage, UI composer. | Uploaded file record, object path, linked entity. | Upload file, refresh, file remains attached. **Done** via tests/test_file_upload.py + web build. |
| Document intelligence | Parse/summarize/compare/extract PDFs, DOCX, PPTX, XLSX, CSV, code, text. | Implemented (Phase 7): on top of the existing `parsing/engine.py` + `doc.parse`/`doc.read`, added broker-routed `doc.summarize` and `doc.compare` (provider `doc`, run through `tool_broker.execute` with permission + audit). Each produces an LLM summary/comparison **with citations validated against the parsed source** — every claim's verbatim quote is located in `full_text` and any claim whose quote is not found is dropped from BOTH the citation list AND the returned summary/comparison output (no fabricated spans). Unparseable docs and LLM-infra failures return an honest warning with no fabricated content; parser warnings (`note`/`parser_used`/`truncated`) surfaced in tool results. Proof: tests/test_document_intelligence.py (PDF/DOCX/XLSX/CSV summarize+compare with `full_text[start:end]==quote` span checks, discriminating fabricated-quote drop tests, unparseable + wrong-shape-JSON + LLM-failure honest-degrade), no regression in tests/test_doc_parsing.py. | Parsers, ingestion, LLM, artifacts. | Extracted text/tables/slides and parser warnings. | Tests per file class with cited extraction. **Done** via tests/test_document_intelligence.py. |
| Image input | Vision for screenshots, diagrams, charts, UI inspection, OCR. | Implemented (Phase 7): true multimodal chat — `core/llm.model_supports_vision` marks vision-capable chat models (gpt-5.4-mini/nano), and `_build_vision_blocks` (chat router) resolves org-owned image attachments into OpenAI-style `image_url` base64 content blocks assembled into the user turn via `core/context.build_user_turn_content`, so the model reasons over the actual image. Tenant-scoped (cross-org images excluded), per-image size cap (5MB) to stay within provider body limits. Honest degraded path when the model lacks vision: OCR-extracted text is embedded in the turn and a truthful "vision unavailable — used OCR" note is attached **only when extracted text exists** (no overclaim). Persisted image attachments render as thumbnails in the chat message. Proof: tests/test_image_input.py (vision path builds image_url block decoding to stored bytes; non-vision → OCR text + honest note, no fabricated block; cross-org excluded via the real `_build_vision_blocks`; oversized skipped; `model_supports_vision` booleans), `npm run build`; no regression in test_chat_turn/test_file_upload/test_llm_and_memory. | LLM provider abstraction, upload, message renderer. | Image attachment and extracted metadata. | Screenshot question returns grounded answer. **Done** via tests/test_image_input.py. |
| Image generation | Generate images with style/size/count controls. | Implemented (Phase 7 + production provider closure): broker-routed `image.generate` (provider `image` in `tool_broker._route`) → `connectors/image_gen.py` provider abstraction behind `_call_provider`. `openrouter/` models use the dedicated `POST /api/v1/images` contract in `connectors/openrouter_multimodal.py`; other provider IDs retain LiteLLM. The production default is `openrouter/google/gemini-3.1-flash-image`. Generated PNGs are saved as `kind="image"` artifacts with generation metadata. Broker enforces max count 4; because the recommended endpoint advertises `n=1`, multi-image requests are bounded sequential calls. Honest degraded leaf: missing model/key, provider error, malformed/base64/non-PNG/empty result all return truthful unavailable/error status with NO fabricated image. Chat **Image mode** invokes the broker and renders resulting artifacts inline. Proof: tests/test_image_generation.py plus mocked dedicated-endpoint contracts in tests/test_openrouter_multimodal.py; no billed generation is part of CI. | Image provider tool, artifacts. | Image artifact and generation metadata. | Generate image artifact and reopen; run a controlled billed production smoke test before launch. **Implemented; live billed proof remains an operator launch check.** |
| Image editing | Edit/variation/masking/background operations where provider supports. | Implemented (Phase 7 + production provider closure): broker-routed `image.edit` resolves and tenant-checks the source, then OpenRouter models send a data-URL `input_references` full-image edit to the same dedicated Images endpoint; non-OpenRouter models retain LiteLLM. Output becomes a **new NON-DESTRUCTIVE version**, preserving prior bytes and history. Masked edits are explicitly unsupported by OpenRouter's dedicated contract and fail before provider I/O; Chronos never discards the mask and pretends a masked operation succeeded. Full-image edits, variations, and prompt-directed background changes remain supported. Honest provider/empty/version-creation failures create no new version. Proof: tests/test_image_editing.py plus full-image-reference and no-HTTP mask contract tests in tests/test_openrouter_multimodal.py. | Image provider tool, artifact versions. | Source image, edit params, output artifact/version. | Full-image edit creates a new version; masked edit reports unsupported without mutation. **Implemented; provider mask support remains unavailable.** |
| Voice input/output | Speech-to-text, TTS, hands-free mode, audio artifacts/transcripts. | Implemented in the repository. Broker-routed STT/TTS, tenant-scoped audio/transcript artifacts, audio upload/live mic capture, composer transcript fill, and per-message playback retain truthful provider failures and redaction. Hands-free mode now uses bounded Web Audio silence detection to record, transcribe, send, speak the reply, and resume listening until explicitly stopped; recording/mic/blob/audio resources are cleaned up and each capture has a hard duration ceiling. | Voice provider, chat UI, storage. | Audio attachment, transcript message, TTS metadata/session state. | `tests/test_voice.py`, `tests/test_openrouter_multimodal.py`, and `apps/web/e2e/client-readiness-static.spec.ts` prove contracts/wiring. Real microphone permission plus billed record/transcribe/respond/playback persistence remains a final Computer Use release exercise. |
| Data analysis workspace | Python-backed analysis of CSV/XLSX/JSON with tables, charts, reports. | Implemented in the repository. Datasets are project-scoped and membership-filtered; CSV, JSON, and XLSX inputs normalize to the stable CSV sandbox contract; production dispatches analysis to a deny-egress ephemeral E2B runtime rather than the API container. Schema/row inference, tables, stdout reports, chart artifacts, tenant/project isolation, source limits, and failure states are wired through the broker and UI. Development retains the bounded subprocess path only for local testing. | E2B data sandbox, data UI, artifacts. | Project-bound dataset record, normalized source, generated charts/reports. | `tests/test_data_analysis.py` proves project isolation, format normalization, sandbox validation, and persisted report/chart artifacts. A real production E2B template/key and live CSV/XLSX/JSON run remain launch checks. |

## Connectors and Apps

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Connector directory | App catalog with setup, scopes, actions, risk, health, policy, audit. | Implemented (Phase 8): `/connectors/catalog` exposes setup env vars, auth type, scopes, actions, risk levels, sync support, policy text, connected account, health status, and last-used state; the Connectors UI renders this metadata and supports OAuth connect/reconnect/disconnect while being truthful for non-OAuth setup. Framework connector install→actions→policy→queued execution→health→audit is proven deterministically. Proof: apps/web/e2e/connectors.spec.ts (directory + connected app), tests/test_connector_phase8.py, tests/test_connector_framework.py, tests/test_connector_operations.py. | Connectors UI/API, repository, health, execution logs. | Connector definitions, installations, policies, health, last-used audit logs. | Connect app fixture, see health/actions/risk/sync/policy, install and execute through framework. |
| Gmail | Search/read and draft/send-with-approval. | Implemented at the governed connector seam. Search/read/draft use the dedicated adapter; send is a hard-floor approved, draft-first operation. Durable state is written into the tenant/task/member-bound approval before provider send; retries replay success, send an existing draft, or recover from Gmail `SENT` evidence, and stop on ambiguous state rather than risk a duplicate. Direct Google and Composio member-scoped paths are covered. | Gmail connector, durable delivery store, broker, approvals, connector directory. | Member credential, exact approved envelope hash, provider draft/message IDs, idempotency evidence, audit/health metadata. | `tests/test_gmail_send_delivery.py` plus existing Gmail/catalog tests. A real connected-account search/draft/approve/send/retry smoke remains required. |
| Google Drive | Search/read/create files; synced knowledge. | Implemented at generic Phase 8 connector level: catalog + OAuth setup, framework read/search/write action schemas, approval-required write action, sync-supported metadata, and connector-source indexing path for cited knowledge. Provider-specific live Drive endpoint smoke remains credential-dependent. | OAuth connector, framework adapters, sync/index. | Drive source records, action schemas, source chunks/citations. | Framework action schema/policy proof + connector indexed citation proof. |
| Google Calendar | Read/create/update events with approval policy. | Implemented at generic Phase 8 connector level: catalog + OAuth setup, framework read/search/write action schemas, and approval-required write action. Provider-specific live Calendar event smoke remains credential-dependent. | OAuth connector actions, framework policy. | Calendar action records, approvals, audit/health. | Framework action schema/policy proof. |
| Slack/Teams | Search/read/send/publish agents to channels. | Implemented at generic Phase 8 connector level for both Slack and Teams: catalog entries, OAuth setup, framework read/search/write actions, external-message risk where applicable, sync-supported metadata, and connector-source research citations. Agent publishing is covered by later Agent Publishing row. | Connectors, framework adapters, source sync. | Channel/message action schemas, message events, indexed source records. | Framework action schema/policy proof + connector indexed citation proof. |
| GitHub | Search/read repos/issues/PRs, code tasks, PR creation. | Implemented at generic Phase 8 connector level: catalog + OAuth setup, framework read/search/write actions, policy/audit/health, and sync-supported metadata. Full repo workspace/PR workflow remains owned by Coding Agent rows. | GitHub connector, framework adapters, coding agent later. | Repo/read/write action schemas, audit/health. | Framework action schema/policy proof. |
| Notion/Linear/HubSpot/Airtable/Jira/Salesforce/Stripe | Typed read/write actions with health and policies. | Implemented (Phase 8): all listed providers are present in the catalog and seeded as framework connectors with read/search/write action schemas, risk levels (including financial/external-message where needed), approval-required write paths, health updates, and audit logs. Proof: tests/test_connector_phase8.py, tests/test_connector_framework.py, tests/test_connector_operations.py. | Connector adapters. | Action schemas, approvals, audit, health. | Per-connector catalog/framework smoke for read/search and approval-gated write. |
| Custom MCP | Remote MCP setup, discovery, allow/deny, health, policy. | Implemented at the governed repository seam. Remote HTTPS server registration, encrypted configuration, discovery logs, policy, health, revocation, read-only research selection, and general discovered-tool invocation are present. `platform.invoke` resolves the discovered action and re-enters `tool_broker.execute`, so per-tool permission, risk, approval, durable-write/idempotency, audit, and untrusted-result controls cannot be bypassed. Local `stdio` remains development-only. | MCP connector framework, platform connector, tool broker. | Remote server config, discovered tools/annotations, policy, discovery logs, per-tool execution and write evidence. | `tests/test_platform_connector.py`, connector-write-ledger tests, and broker approval tests cover discovery/invoke/connect. Production remote credentials plus revoke/health/invoke browser proof against a live MCP server remain release evidence. |
| Custom HTTP/OpenAPI | User-defined API connector from OpenAPI/manual schema. | Implemented in the repository for manual schemas and signed inbound webhooks. Admins can define tenant-scoped HTTPS actions and encrypted credentials in the Connectors UI; redirects and private/non-HTTPS targets are blocked, response sizes are bounded, content is scanned as untrusted, health/revocation are explicit, and mutating/destructive methods retain approval floors. Inbound endpoints use one-time HMAC secrets, timestamp/replay/rate/payload limits, encrypted payload persistence, metadata-only audit, rotation/disable/test controls, and database-idempotent workflow starts. OpenAPI-document import remains an enhancement rather than a prerequisite for manual schema operation. | Connector builder/framework, vault, workflow runtime, Connectors admin UI. | Encrypted credential reference, action schema/risk, health/revocation state, webhook secret hash/version, replay/idempotency records, encrypted delivery payload, workflow linkage, audit evidence. | `tests/test_custom_integrations.py` and `apps/web/e2e/custom-integrations-static.spec.ts` prove lifecycle, SSRF/redirect denial, approval floors, HMAC/timestamp/replay behavior, encryption, redaction, idempotent workflow start, and the accessible admin UI. A real production endpoint plus signed public delivery remains a live launch proof. |

## Browser, Computer, and Autonomous Operation

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Browser search/fetch | Web search/fetch with screenshots and fallback honesty. | Implemented at the broker/provider seam with live-provider and truthful unavailable/degraded envelopes. Search/fetch evidence is scanned as untrusted before model use. Production credentials still require live quota/error-state proof. | Browser connector, broker, provider health. | Provider/tier/warning metadata, citations, optional screenshot reference. | Existing browser/search/Tavily tests plus a controlled live search/fetch and provider-outage exercise on the deployed release. |
| Full browser operator | Navigate/click/type/select/scroll/wait/extract/download/upload/read DOM. | Implemented in the repository on a production-only Browserbase runtime. Tenant-pseudonymous Browserbase Contexts retain login state; Postgres stores only opaque remote IDs and session/event metadata; an advisory lock serializes cross-replica actions; screenshots and size-bounded, malware-scanned downloads are saved in tenant-scoped object storage; task-workspace uploads are scanned before browser handoff; close/revoke deletes the remote context. Local Chromium is development-only and forbidden in production. | Browserbase session manager, broker tools, ClamAV sidecar, Postgres, S3, UI. | `browser_sessions`/events, opaque context/session IDs, durable scan-tagged screenshots/downloads, consent and takeover state. | `tests/test_browser_operator_phase9.py` covers remote creation/rehydration, durable capture/download, tenant scope, plaintext-state purge, and the production local-runtime ban; `tests/test_file_security.py` covers the scan boundary. A real Browserbase navigate/form/clean-and-malicious-download/restart run is still required. |
| Live browser view | User sees browser session and task actions. | Implemented at the UI/API seam: Activity shows current screenshot/state/event replay/downloads, and takeover requests can obtain a short-lived Browserbase live-view URL that is never persisted or logged. Source-contract/build proof is present; a live remote feed has not been validated for this release. | Browser UI, Browserbase debugger/live view. | Tenant-checked short-lived URL, durable screenshots and replay events. | `apps/web/e2e/browser-operator-static.spec.ts` is source-contract proof only. Add Computer Use/live Browserbase proof for opening, takeover, hand-back, refresh, and revoke. |
| User takeover | Pause automation for user click/type/MFA/CAPTCHA, then hand back. | Implemented at the state/API/UI seam: request reason, takeover state, short-lived live view, user summary, hand-back, revoke, and durable events. MFA/CAPTCHA completion itself must be performed by the user and has not been exercised against the current production configuration. | Browser UI, Browserbase live view, runtime pause/resume. | Takeover request/release state and hand-back summary. | Backend state tests plus a live user takeover/MFA hand-back exercise on the deployed release. |
| Authenticated browser sessions | User-consented saved sessions, revocation, per-task approval. | Implemented with explicit purpose, allowed domains, bounded expiry, sensitive-site approval, tenant/member/task binding, revocation, and Browserbase Context deletion. Raw cookies/storage state are not persisted by Chronos. Network/redirect safety checks and consent are enforced, but a live saved-session/revocation exercise is still outstanding. | Browser profile/session manager, settings/UI. | Opaque remote context ref, consent, approvals, last used, expiry/revocation. | `tests/test_browser_operator_phase9.py` plus a live two-member isolation, saved-login, sensitive-site approval, expiry, and revoke proof. |
| Cloud computer | Sandboxed terminal/files/browser/editor/package install/artifact export. | Production-isolated E2B desktop implementation: broker-visible `computer.*` tools require a human-approved purpose/capability/expiry envelope, create a tenant-bound Linux desktop from the dedicated template, and persist only server-side sandbox metadata. Each operation revalidates provider ownership metadata and resumes the same E2B sandbox across replicas. Idle sessions auto-pause; the leader-only expiry job permanently destroys retained state at consent expiry. Terminal/files/packages, real PNG screenshots, bounded mouse/keyboard input, pause/resume/cancel, and durable artifact export are available only when consent includes the relevant capability. Creation, shell/package use, raw GUI input, and destructive cancellation retain hard approval floors. `/computer-sessions` and the Activity Computer UI expose current pixels, controls, expiry, policy, and durable replay without exposing the provider sandbox id. | E2B 2.x desktop template, broker, Postgres, Redis leader/quota coordination, artifact storage, UI. | `computer_sessions`/events store tenant/member/task binding, consent, capability and hard expiry, opaque provider id server-side, network/resource policy, status, command/input audit, and artifact ids. | `tests/test_computer_phase10.py` proves consent/approval, tenant isolation, terminal/files/export, real screenshot contract, bounded input, provider-expiry honesty, pause/resume/cancel, and hard consent expiry; `tests/test_production_execution_boundary.py` proves host isolation/config; `apps/web/e2e/computer-static.spec.ts` proves the visible contract. A launch gate still requires a real E2B desktop template/key plus live screenshot/input/resume smoke evidence. |
| Local computer bridge | Desktop bridge with authorized folders/apps/commands/local compute. | Implemented in source and local packaging. Production routes `local_computer.*` to a paired macOS device rather than the API host. Pair codes are one-time/short-lived, device secrets and bookmarks stay in Keychain/on-device, the server stores only opaque grants, commands are signed/leased/replay-protected, folder paths are jailed, risky commands/apps remain approval-gated, result retry is idempotent, revoke cancels outstanding work, and narrow native notifications are supported. | macOS menu-bar app, desktop-device API, broker/policy, web device settings. | Device registration, encrypted secret, opaque folder grants, signed commands/results, audit/events. | `tests/test_desktop_device_bridge.py`, Swift `--self-test`, `apps/web/e2e/desktop-task-controls-static.spec.ts`, and local ad-hoc package verification. A Developer ID-signed/notarized release, paired production-device flow, notification permission, and revoke smoke remain required. |
| Long-running delegated work | Manus-style task console with plan, live actions, artifacts, pause/resume/cancel. | Implemented in the product UI/runtime. The activity drawer shows live status, plan, progress, reasoning/tool events, reconnect state, artifacts, and pause/resume/cancel. The full Activity view filters jobs/actions, expands durable timelines/results/assignments, exposes approval-aware controls, retries dead-letter work, and resumes from server state after refresh instead of relying on transient browser state. | Activity UI, task controls, runtime trace, retry/recovery APIs. | Durable task timeline, plan/progress/events, artifacts, assignments, pause/cancel state, retry/dead-letter evidence. | Runtime reliability/task-control suites and `apps/web/e2e/tasks-audit-data-static.spec.ts` prove the contract. A long deployed task must still be paused, refreshed, resumed, and completed under Computer Use before release sign-off. |

## Coding Agent

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Repo workspace | Clone/import repos, create branch, inspect/edit/test/diff. | Production-isolated runtime implementation: broker-visible `repo.*` tools use a tenant/task-bound persistent E2B sandbox, Postgres cross-replica lease, bounded command/file/workspace quotas, and versioned S3 snapshot recovery. Public GitHub URLs clone directly. When a member connects the dedicated GitHub OAuth app, the API fetches private source bytes and uploads the archive to E2B without transporting the vault token into the sandbox. Local host paths remain development-only. Model/user values travel in JSON control files and subprocesses receive argv; they are never shell-interpolated. | Coding runtime, GitHub OAuth/vault, E2B, Postgres, S3, chat runtime. | Opaque workspace id, tenant/task/path binding, sandbox id (server-only), snapshot pointer/version, lease/expiry, source metadata, test/diff/status output. | `tests/test_repo_workspace_production.py` proves isolated dispatch, no URL shell interpolation, snapshot persistence, expired-sandbox restore, path denial, and migration invariants; local red/green loop remains in `tests/test_repo_workspace.py`. Live E2B/GitHub isolation, quota, and restart evidence remains a launch gate. |
| Commit/PR flow | Commit and open PR under approval/policy. | Implemented in the repository. `repo.commit` runs inside the persistent isolated workspace and snapshots Git state. `repo.create_pr` remains a broker hard-floor approval action, validates the approved request against a clean committed workspace and immutable source/base SHAs, creates a non-force remote head, atomically appends a GitHub-signed commit through `createCommitOnBranch`, and opens the real PR. The direct member OAuth token stays API/vault-only and is cleared after use. Durable stage/provider evidence adopts marker-bound remote commits or PRs after crash/replay without duplicating writes or overwriting unrelated branches. | GitHub OAuth/vault, broker, approvals, isolated repo workspace, durable publication state. | Source/base/head SHA, approved request hash, internal approval/idempotency hashes, publication stages, provider commit/PR IDs and URL, snapshotted non-secret PR artifact. | `tests/test_repo_workspace_production.py` plus focused GitHub publisher/broker tests prove branch/commit/PR creation, token non-persistence, scope enforcement, crash adoption, replay, and the hard floor. A real authorized GitHub repository publication remains a live launch proof. |
| Code review | Review PR/issues with inline findings and suggested patches. | Implemented (Phase 11 local review artifact): `repo.review` reviews the current diff/workspace, emits inline findings with file/line/severity/body/suggested_patch, summarizes changed files, and persists `.chronos/code_review.json`; chat/tool traces expose the review result. Remote PR comment publishing remains credential-dependent. | Coding agent, GitHub connector, chat UI. | Review artifact with inline findings and suggested patches. | `apps/api/tests/test_repo_workspace.py::test_repo_workspace_import_list_status_commit_pr_and_review` plus `apps/web/e2e/coding-agent-static.spec.ts`. |
| Test/debug loop | Run tests, inspect failures, patch, rerun, summarize. | `repo.run_tests` accepts only validated pytest argv, rejects shell operators and path escapes, caps runtime/output, and executes production code only inside the dedicated E2B repo profile. Mutating operations snapshot state so a later replica or replacement sandbox resumes the red/green loop. | Isolated coding runtime, repo workspace, chat UI. | Test command status/stdout/stderr, diff, status, review/commit artifacts, snapshot version. | `tests/test_repo_workspace.py` plus `tests/test_repo_workspace_production.py`. |

## Scheduled Work, Workflows, and Agents

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Scheduled tasks | One-time/daily/weekly/monthly/interval/event-triggered tasks. | Implemented (Phase 12): schedule API now supports one-time, daily, weekly, monthly, interval, webhook, and connector-trigger schedules, including paused/active state, next-run calculation, manual run, APScheduler polling, task materialization through the existing task runtime, run history rows, and audit events. Proof: `apps/api/tests/test_scheduled_tasks.py` plus Workflows UI static guard. | Scheduler, tasks UI/API. | `scheduled_tasks`, `scheduled_task_runs`, timezone-normalized next run, trigger metadata, `/schedules` API, `/workflows` UI schedule panel. | Schedule kinds compute correctly, due schedules spawn real tasks, paused schedules skip, run history is recorded. **Done** via `pytest apps/api/tests/test_scheduled_tasks.py` and `apps/web/e2e/workflows-static.spec.ts`. |
| Workflows | Convert tasks to reusable workflows with steps/dependencies/conditions/approvals. | Implemented (Phase 12): workflow definitions accept reusable connector steps, dependencies, conditions, retry metadata, and trigger definitions; workflow run APIs expose manual start, event dispatch, pause/resume/cancel, recovery, run detail, and lifecycle history. Existing chat convert-to-workflow remains wired to the same runtime. Proof: `apps/api/tests/test_phase12_workflows_monitors.py`, Workflows UI static guard. | Workflow runtime/UI. | `workflows`, `workflow_runs`, `workflow_steps`, `workflow_step_dependencies`, `workflow_state`, `workflow_triggers`, `workflow_run_events`, `/workflows` API/UI. | Create workflow, persist triggers, dispatch matching event, pause/resume/recover, inspect run history. **Done** via focused backend and static UI tests. |
| Monitors | Watch sites/sources/connectors/inbox/news and alert or run task. | Implemented in the repository. Durable website, news/search, inbox, and read-only connector polling uses Postgres lease claims, per-org quotas, restart-safe run keys, deterministic jitter, conditional ETag/Last-Modified requests, bounded no-redirect HTTPS fetches, SSRF re-resolution with proxy environment disabled, and broker-routed read-only connector tools. Results are scanned as untrusted; injection-marked content cannot start workflows. Retries reuse the same run through backoff/dead-letter history, while alert hashes/cooldowns prevent duplicates. Alerts, notifications, and optional workflow triggers are audited/idempotent. Workflows UI exposes cadence, run-now, pause/resume, health, last/next run, failure history, and alerts. | Monitor poller, leader scheduler, broker/connectors, notification/workflow runtime, Workflows UI. | Extended `monitors`, durable `monitor_runs`, deduplicated `monitor_alerts`, lease/run/error/observation/provider metadata, notification/workflow linkage. | `tests/test_monitor_polling.py`, `tests/test_phase12_workflows_monitors.py`, and `apps/web/e2e/monitor-polling-static.spec.ts` prove lease exclusion, manual-run idempotency, alert dedupe, retry/dead-letter, SSRF/proxy/error redaction, degraded state, and accessible controls. Live production provider egress and change-to-delivery evidence remains a launch proof. |
| Agent profiles | Reusable agents/personas with instructions/tools/projects/memory/autonomy/policy. | Implemented (Phase 13): durable `agent_profiles` store role, instructions, model, tool grants, connector grants, project bindings, memory scopes, autonomy level, approval policy, schedule permissions, lifecycle status, audit, and profile events. `/agents/templates` exposes research, executive assistant, sales/SDR, support, engineering, data analysis, and operations templates. `/agents` UI is now a real agent builder/run/publish workspace instead of an empty panel. Agent runs create normal governed Chronos tasks with the profile embedded in task state and project/memory policy preserved. Proof: `apps/api/tests/test_agents_phase13.py`, `apps/web/e2e/agents-static.spec.ts`, `npm run build -- --webpack`. | Agents API/UI, settings, runtime. | `agent_profiles`, `agent_profile_events`, profile CRUD, run endpoint, tool grants, connector grants, project bindings, memory scopes, approval/schedule policy, task state profile snapshot. | Create agent, attach project/tool/memory scope, run constrained task, cross-org read blocked, UI route/build proof. **Done** via tests above. |
| Agent publishing | Publish agents to Slack/Teams/email/web/API. | Implemented in the repository. Tenant-scoped publication bindings and vault-held rotating secrets support publish/unpublish/rotate/revoke, allowed web origins, API/web rate limits, signed Slack/Teams/SendGrid inbound verification, replay-safe inbound event and external-conversation linkage, normal governed Chronos tasks, and durable approval-gated outbound delivery receipts. Slack/Teams use member-scoped active connector bindings; email requires the configured inbound/outbound provider; web/API return only after the policy gate. Existing publications whose original plaintext token could not be re-keyed migrate to an explicit `needs_rotation` state. Migration 0068 forward-reconciles already-stamped databases whose 0064 notification checks and approval linkage predated the final contract. The Agents UI manages bindings, publication lifecycle, one-time credentials, origin/rate policy, and degraded provider state. | Agent publication service, provider bindings, notification delivery, vault/connectors, Agents UI. | `agent_publication_bindings`, vault refs/fingerprints/version, publication lifecycle/provider status, external conversation/task links, replay-safe inbound events, approval-linked delivery receipts, provider metadata. | `tests/test_agents_phase13.py`, `tests/test_agent_publication_receipts.py`, `tests/test_publication_migration_reconcile.py`, and `apps/web/e2e/agents-static.spec.ts` prove repository contracts. Real Slack/Teams/email install/signature/inbound/outbound/retry/revoke plus web-origin/API-token exercises on the deployed accounts remain launch proof. |

## Collaboration, Admin, Mobile, and Compliance

| Capability | Target parity | Current state | Implementation area | Interface and persistence | Acceptance proof |
|---|---|---|---|---|---|
| Shared conversations | Share chats with members/projects and role-based visibility. | Implemented: migration 0049 adds creator-private `conversation_members` ACLs with atomic owner seeding/backfill; viewer/editor/owner roles are enforced by the canonical `core/conversation_access.py` policy across chat reads/writes, search, project lists, artifacts, memory scopes, attachments, and chat-history tools. `GET/PUT/DELETE /chat/conversations/{id}/members[/member_id]` manages same-tenant active-member grants, mirrors roles to OpenFGA, and emits audit + targeted notifications. The UI now shows shared state, lets the immutable owner add/remove teammates and change viewer/editor access, lets editors contribute and rename, and fail-closes the composer plus message mutations for viewers or unverifiable ACL state. A minimal active, same-tenant `/settings/member-directory` supplies teammate ids without administrative metadata. | Chat/projects/permissions + collaboration UI. | `conversation_members` ACL records and minimal member directory projection. | `tests/test_collaboration_acl_handoff.py` proves private default, minimal tenant-safe member discovery, viewer read-only, editor write, cross-tenant denial, notification/audit, and revoke; `apps/web/e2e/collaboration-static.spec.ts` plus lint/typecheck/production build prove the UI contract. **Done.** |
| Comments and mentions | Comments on projects/artifacts/tasks, mentions, assignments, handoff. | Implemented: migration 0044 + `core/comments.py` provide tenant/member-safe comment threads and access-filtered `@mentions`; migration 0049 adds the current task assignee and append-only `task_assignment_events`. The UI exposes real comment threads and mention affordances on project, task, and artifact targets; task responsibility controls cover assign, reassign, assignee handoff with optional context, owner unassign, current assignee visibility, and readable append-only history. Mutations use the implemented `PUT/GET/DELETE /tasks/{id}/assignment`, `GET /tasks/{id}/assignment/history`, and `POST /tasks/{id}/handoff` APIs and preserve immutable creator ownership. | Collaboration UI, task activity, projects, artifacts. | Comment, mention, current assignment, append-only handoff records. | `tests/test_comments.py` and `tests/test_collaboration_acl_handoff.py` prove mention filtering, assign/handoff/unassign lineage, current-assignee visibility, prior-assignee revocation, cross-tenant denial, notifications, and audit; `apps/web/e2e/collaboration-static.spec.ts` and the production build prove all supported UI targets and mutation routes. **Done.** |
| Admin/RBAC | Members, roles, groups, workspaces, projects, agents, policies, retention. | Implemented in the repository for administrative lifecycle. In addition to invitations, SSO/SCIM, roles, projects, policies, retention, and holds, owners/admins manage tenant-scoped native groups and workspaces, workspace membership/owner invariants, archive/restore, legal-hold-aware delayed deletion/cancellation/tombstones, ownership transfer, and leave/deactivation. Organization API keys are one-time-display, vault-key-peppered, scoped/expiring/rate-limited, atomically rotated/revoked, audit-redacted, and immediately disabled with their creator. Migration 0066 binds every conversation to a tenant-native workspace with a composite tenant foreign key. The chat UI lists only accessible workspaces for new conversations, locks an existing conversation to its stored workspace, and disables sends for archived/unavailable workspaces. | Settings/admin, native lifecycle, workspace access, permissions/OpenFGA, auth/API keys, retention, member/connector/workflow/chat creation. | Native groups/members, workspaces/members/status/tombstone, conversation workspace identity, organization API-key hash/scope/expiry/use metadata, authorization tuples, audit evidence. | `tests/test_admin_lifecycle.py`, chat/project/settings/auth/workflow/custom-integration regressions, and `apps/web/e2e/client-readiness-static.spec.ts` prove tenant boundaries, owner guards, key controls, workspace backfill/access/state, conversation binding, destructive confirmation, and UI wiring. Complete deployed role/lifecycle and non-default workspace browser smoke before release. |
| Audit/compliance export | Export audit, connector access, memory access, approvals, task reports. | Implemented. Owners/admins can create a tenant-scoped durable JSON artifact containing redacted evidence across all five categories, a SHA-256 event chain, HMAC-signed manifest, filters/counts, and authenticated download path. Raw goals/action payloads are hashed and connector secrets are omitted. | Compliance export API, audit service, artifact UI. | Durable export artifact and tamper-evident manifest/events. | `tests/test_compliance_export.py` proves tenant scope, role floor, redaction, durability, and verification; `apps/web/e2e/compliance-export-static.spec.ts` proves UI wiring. A deployed authenticated download remains launch proof. |
| Responsive mobile web | Core flows work on mobile viewports. | Implemented at the responsive-code/fixture-test level for the app drawer, chat attachments, Activity tabs, approvals, connectors, artifacts, settings, login/signup/onboarding, modal focus, and horizontal overflow. It has not received exhaustive Computer Use validation against real API states, uploads, virtual keyboard, slow/error states, or multiple physical browsers. | Frontend. | Same APIs and route state; responsive layouts. | `apps/web/e2e/mobile-responsive.spec.ts` runs Pixel 7 Chromium and iPhone 15 WebKit against a production Next bundle with deterministic API fixtures. Complete real-data mobile Computer Use and behavioral flow proof before release. |
| Desktop app | Desktop shell for local bridge, notifications, folder auth. | Implemented in source and locally packageable: sandboxed Swift menu-bar app, pairing, Keychain secrets, security-scoped folders, global shortcut, tray status, signed command polling/results, and native notifications. The checked-in/local DMG is ad-hoc evidence only; no current Developer ID-signed, notarized, stapled GitHub release is proven. | macOS app, desktop bridge, web device settings, release workflow. | Device/grant state, Keychain secrets, package/notarization artifacts. | Swift build/self-test, backend bridge tests, and ad-hoc codesign are present. Configure protected Apple secrets, run `desktop-release.yml`, notarize/staple, install on a clean Mac, pair/execute/revoke/notify, and preserve evidence. |
| Notifications | In-app/email/Slack/Teams/desktop notifications for approvals/tasks/monitors. | Implemented in the repository. Tenant/member-scoped in-app inbox/read/dismiss, settings gates, signed paired-mac delivery, SendGrid email, and active member-scoped Slack/Teams bindings materialize durable per-recipient claims. Provider dispatch supports Composio-managed and direct Slack/Teams connectors, deterministic dedupe keys, bounded claims, exponential retry/dead-letter, explicit ambiguous-provider dead letters, and idempotent weekly email digests under leader election. Agent responses reuse the same receipt/claim machinery with approval linkage and thread metadata. No verified-domain/provider or packaged-desktop delivery evidence exists for this release. | Notification service, scheduler, SendGrid, Slack/Teams connector bindings, desktop bridge. | Notification rows/read receipts, channel-bound delivery receipts, retry/dead-letter/approval state, digest periods, bounded provider metadata. | `tests/test_notifications.py`, `tests/test_notification_delivery.py`, `tests/test_agent_publication_receipts.py`, and desktop bridge tests prove repository behavior. Run live preference/action-link, email/Slack/Teams delivery, controlled retry/dead-letter/revoke, digest, and packaged-desktop proof before release. |
| Billing/plan controls | Enterprise plan/account controls if commercialized. | Implemented in the repository with a truthful disabled state when the four-value Stripe tuple is absent. Admin checkout/customer portal, tenant-bound customer/subscription records, server-owned metadata, Stripe idempotency keys, signed webhook parsing, configured-price entitlement mapping, event deduplication/order protection, cancellation reconciliation, usage, and billing UI are present. No live Stripe products/prices/portal/webhook/account verification is proven. | Stripe integration, billing API/UI, plan governance. | `billing_accounts`, `billing_webhook_events`, plan/usage/entitlement state. | `tests/test_billing.py` proves configuration, checkout/portal, signatures, idempotency, tenant binding, and reconciliation. Run live test-mode then production checkout/portal/webhook/refund/support evidence before launch. |
| Accessibility and interaction polish | Keyboard/focus/ARIA/readability/error states across the complete product. | Partial. Shared focus helpers, modal focus trapping/restore, Escape handling, tab semantics, accessible names, live regions, mobile drawer behavior, route-level loading/error/not-found surfaces, and responsive source contracts are present. No axe audit, screen-reader pass, contrast audit, browser zoom/reflow proof, or exhaustive keyboard/Computer Use walkthrough exists. | Frontend design system and every route. | Reusable accessible primitives and route/state contracts. | `apps/web/e2e/accessibility-static.spec.ts` is source inspection, not an accessibility conformance test. Add automated axe plus VoiceOver/keyboard/zoom/high-contrast evidence for every route and modal. |
| First-run setup and runtime health | Guided client setup and one operator view of queue/provider/runtime health. | Implemented in the repository. Authenticated `/settings/runtime-health` separates launch-blocking services from optional capabilities and reports database, Redis, object storage, ClamAV file security, OpenFGA, worker freshness/count, identity, model, E2B browser/computer/repo, connectors, email, billing, and observability without exposing secrets or worker internals. Admins can request audited live verification, and onboarding completion fails server-side with `runtime_not_ready` while required services are unavailable. After readiness and least-privilege team invitations, the wizard presents a connector → project → source → research → approval → schedule guide. `/settings/onboarding/guide` derives each completion state from tenant-scoped durable records, and the same live checklist remains on the empty-chat welcome surface instead of relying on browser-local completion claims. | Runtime health aggregator, onboarding, first-use guide, settings/admin, connector health, observability. | Server-evaluated required/optional checks, durable tenant-scoped first-use evidence counts, worker/scanner/provider health, audited admin refresh. | `tests/test_runtime_health.py`, `tests/test_onboarding.py`, `tests/test_file_security.py`, and `apps/web/e2e/runtime-health-static.spec.ts` prove redaction, role gating, required/optional behavior, server-enforced completion, tenant-scoped guide state, and UI wiring. Complete the guided path against deployed provider accounts and preserve browser evidence before release. |

## Final Parity Proof Scenarios

None of these combined scenarios is recorded as passing on the current
immutable production release. Individual backend/UI slices exist, but they do
not substitute for the end-to-end deployed proof below.

| Scenario | Required flow | Required proof |
|---|---|---|
| ChatGPT-style parity | Upload CSV and chart/image, analyze both, create cited answer, chart/report artifacts, regenerate/branch. | Playwright + API proof with persisted files, artifacts, branch lineage, and citations. |
| Claude-style parity | Create project, upload docs, ask project-grounded question, cite sources, create/edit/version artifact, save/inspect memory. | Project source citations, artifact version history, memory usage proof, refresh persistence. |
| Manus-style parity | Browser task navigates/clicks/types, pauses for takeover, resumes, downloads file, creates artifact, replays after refresh. | Browser session trace, takeover trace, artifact, and restart/refresh proof. |
| Enterprise governance | Risky write requires approval, unauthorized user blocked, authorized approval resumes exactly once, audit export proves chain. | Approval and audit records plus no duplicate external write. |
| Connector ecosystem | Connected source syncs, source is used in research, permission revocation removes access. | Sync state, citation, revoked access test. |
| Reliability and safety | Task survives API restart, cancellation stops work, prompt injection blocked, degraded connector reported honestly. | Restart/cancel/injection/fallback tests. |

## Completion Workflow

1. Pick a matrix row before implementing a feature.
2. Update the row status only after implementation and proof exist.
3. Add or update tests named for the row's acceptance proof.
4. Link proof artifacts or commands in the relevant PR/implementation summary.
5. Do not mark any row complete based on scaffolding, mocks, or UI-only controls.
